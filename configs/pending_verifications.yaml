# 수정 유효성 자동 검증 등록부 (고도화 B, 2026-08-03)
#
# 여기 적은 항목은 매 거래일 장후에 `logs/daily_integrity_*.json` 이력으로 자동 채점된다
# (`src/messiah/ops/fix_verification.py`). 목적은 하나다 — **"고쳤다"는 판단을 사람 기억이
# 아니라 다음 거래일의 실측이 내리게 하는 것**.
#
# 이게 없어서 2026-07-29~08-03에 같은 UI 크래시를 세 번 "고쳤다"고 판정하고 세 번 재발시켰다.
# 매번 판정 기준 자체는 기록돼 있었지만(예: "다음 거래일 native_crashes 0건"), 그걸 다음날
# 다시 꺼내 확인하는 일을 아무도 강제하지 않았다.
#
# 규칙:
#   - 등록일(registered) **다음** 거래일부터 채점한다. 수정은 그날 장 마감 후에 들어가므로
#     등록일 리포트는 수정 이전의 세계다.
#   - 한 번이라도 기준을 위반하면 즉시 `재발` — 그게 이 등록부의 존재 이유다.
#   - 판정이 `검증 완료`로 굳으면 이 파일에서 지운다. 안 지우면 매일 통과 줄만 쌓여
#     정작 봐야 할 `재발`이 묻힌다(DECISION_LOG의 "닫히면 태그를 지운다"와 같은 규율).
#
# 지표(metric)는 무결성 리포트에 실제로 있는 필드만 쓸 수 있다:
#   native_crashes · faulthandler_dumps · ui_restarts · restarts
#   critical_log_lines · breaches · missing_minutes · longest_gap_minutes
#   tick_rows  ← min 기준(2026-08-04 신설)
#
# ## 예측은 "좋아진다"가 아니라 "존재한다"부터 적는다 (2026-08-04, 마흐디에서 이식)
#
# **어떤 값을 쓰는 fix라면, 그 값이 실제로 생산되고 있다는 것을 먼저 예측치로 적는다.**
#
# 마흐디가 2026-08-03에 이걸 배웠다: 그날 예측 13개 중 12개가 자동 대조로 즉시 확인됐지만,
# 그 어떤 가설도 `find_gamma_flip()`이 **전 이력에서 한 번도 값을 돌려준 적이 없다**는 사실을
# 잡지 못했다 — 아무도 "감마플립이 계산되는가"를 예측치로 적지 않았고 *당연히 되고 있다*고
# 가정했기 때문이다. 그 결과 앙상블 멤버 하나가 넉 달간 죽어 있었고, 넉 달 동안 "개선"해 온
# 대상이 애초에 존재하지 않았다.
#
# MESSIAH도 같은 형태를 이미 두 번 겪었다 — `px_ema_cross_60`(NaN이라 흔적은 남았다)과
# `px_macd_h_5`(값을 내므로 **아무 흔적도 없었다**, 2026-08-04 피처 관문이 처음 발견).
# 그래서 `max` 기준만이 아니라 `min` 기준 항목을 적을 수 있게 했다.

verifications:
  - id: ui-crash-isolation
    summary: "UI polars 크래시 격리 — 봉 파싱을 자식 프로세스로 분리(P0-1b, 커밋 c790b58)"
    registered: 2026-08-03
    # 판정 기준이 "크래시 0건"인 게 핵심이다. 격리가 성공하면 자식이 죽어도 UI는 사는데,
    # **자식의 죽음도 여전히 네이티브 크래시로 집계된다** — 즉 이 기준은 "영향 격리"가 아니라
    # "원인 소멸"을 본다. 격리만 성공하고 크래시가 계속되면 `재발`로 뜨는 게 맞다: 화면은
    # 살았지만 근본 원인은 그대로라는 사실이 매일 드러나야 한다.
    metric: native_crashes
    max: 0
    # 3거래일 — 07-31 수정은 하루(08-03)만에 깨졌고, 07-30 수정은 당일 재발했다. 하루로는
    # 우연한 조용함과 구분이 안 된다. 이 값 자체는 미검증 초기값이다.
    consecutive_days: 3
    deadline: 2026-08-14

  # 2026-08-06 교체 — **이 항목은 잘못 얻은 "검증 완료"였다.**
  #
  # 옛 지표 `ui_restarts`는 `CommandCenterUIRestarted` 태그, 즉 **인프로세스 워치독의 자동
  # 재기동만** 센다. UI 프로세스가 밖에서 죽는 경로는 구조적으로 시야 밖이다.
  #
  # 2026-08-06에 UI가 10:04~10:25 사라졌는데(호스트 재부팅) 값은 **0**이었고, 이 항목은
  # 그 위에서 "3거래일 연속 검증 완료"를 찍고 있었다. `IntegrityReport.ui_restarts`의 필드
  # 주석은 *"관측 공백의 직접 지표"* 라고 적혀 있는데, 21분짜리 관측 공백을 못 봤다.
  #
  # 지표를 `observation_gap_minutes_max`로 바꾸고 **연속일을 0으로 되돌린다** — 옛 지표로
  # 쌓은 3일은 이 질문에 대한 답이 아니었으므로 그대로 이월하면 안 된다.
  - id: ui-restart-observability
    summary: "관측 공백을 실제로 재는가(P1-1 재정의) — UI 포함 프로세스 공백이 조용히 지나가지 않는지"
    registered: 2026-08-06
    metric: observation_gap_minutes_max
    # 5분: 부팅 트리거 도입 후 재부팅 복구의 설계값이 2~3분이다(부팅 30초 + 트리거 지연
    # 1분 + 기동 30초). 2026-08-06의 21분과는 4배 이상 떨어져 있다.
    max: 5
    consecutive_days: 3
    deadline: 2026-08-19

  - id: crash-forensics-armed
    summary: "faulthandler 상시 무장(P0-1a) — 크래시가 나도 증거가 남는 상태인지"
    registered: 2026-08-03
    # 2026-08-05 정정: 종전엔 `breaches`(넓은 그물)로 채점했는데, 그러면 **무장과 아무
    # 상관 없는 사고 하나가 이 수정을 "재발"로 만든다**. 실제로 08-04 리포트에서 체결틱
    # 0행(그날 결선 전이라 정상) 때문에 ERROR가 찍혔고, 그 전엔 PowerShell이 마커 줄에
    # 접두사를 붙인 것 때문에 또 찍혔다 — 두 번 다 무장은 정상이었다.
    # 이제 "무장 안 된 프로세스 수"만 본다.
    metric: crash_forensics_unarmed
    max: 0
    consecutive_days: 3
    deadline: 2026-08-14

  - id: tick-collection-live
    summary: "체결틱 원본 수집 결선(F2, 2026-08-04) — 결선만 하고 0행으로 끝나지 않는지"
    registered: 2026-08-04
    # **min 기준**이다 — 이 등록부에서 "일어나면 안 되는 일"이 아니라 "일어나야 하는 일"을
    # 재는 첫 항목이다(파일 헤더 "존재한다부터 적는다" 참고).
    #
    # 이 프로젝트는 폴러를 만들고 결선을 안 붙여 데이터를 잃은 전례가 셋 있다:
    # InvestorFlowPoller(7개월) · OptionChainPoller(수개월) · 그리고 FL 피처(모델 미도달).
    # 셋 다 "만들었으니 되고 있겠지"였고, 셋 다 몇 달 뒤에 발견됐다.
    #
    # 틱은 그중 최악이다 — KIS 분봉 API는 OHLCV만 주고 **체결 단위 과거 조회가 아예 없어**
    # 안 쌓인 날은 영원히 빈다.
    metric: tick_rows
    # 1,000행은 매우 느슨하다(실측 초당 3틱 ≈ 하루 5~10만행). "적게 쌓였다"가 아니라
    # **"결선이 끊겼다"**를 잡는 값이다.
    min: 1000
    # 3거래일 — 첫날 성공은 우연일 수 있다(재연결·스톨 경로를 안 탄 하루일 수 있음).
    consecutive_days: 3
    deadline: 2026-08-12

  # ---------------------------------------------------------- 2026-08-05 일일점검 대응

  - id: clock-sync-restored
    summary: "w32time 자동 동기 복구(P0-1) — 로컬 시계가 다시 밀리기 시작하지 않는지"
    registered: 2026-08-05
    # 2026-08-04 실측: 거래소 시각 대비 로컬이 9.72초 느렸고, 하루 4~5초씩 벌어지고 있었다.
    # 원인은 Windows Time 서비스가 `Stopped`/`Manual`이라 부팅해도 안 켜진 것.
    # Automatic + NTP 피어 설정 후 오프셋 0.0006초.
    #
    # **이 항목이 재발하면 서비스가 다시 꺼졌다는 뜻이다** — 값이 서서히 커지는 것이
    # 특징이라 하루치로는 정상처럼 보인다. 그래서 연속일 수를 다른 항목보다 길게 잡는다.
    metric: clock_skew_abs_seconds
    # 2.0초는 완성봉 유예 500ms가 무의미해지기 시작하는 지점(`ops/clock_skew.py`).
    max: 2.0
    consecutive_days: 5
    deadline: 2026-08-19

  - id: horizon-volume-identity
    summary: "종료 시퀀스 경합 제거(P0-2) — 마지막 1분봉이 상위 Horizon에서 안 빠지는지"
    registered: 2026-08-05
    # 2026-08-04 실측: 1분봉 합 84,346 vs 3/5/10/15/30분봉 전부 84,209. 차이 137은 정확히
    # 15:34봉 하나였다. `flush_final_bar()`가 버스에 발행만 하고 돌아온 뒤 곧바로
    # `flush_all_final()`이 돌아, 그 봉이 구독자(합성기)에 도달하기 전에 버킷이 닫혔다.
    #
    # 이 지표가 0이 아니면 셋 중 하나다: ① 종료 경합이 재발 ② 시계 스큐로 버킷이 잘림
    # ③ 1분봉만 백필하고 상위 Horizon 재합성을 안 함. 셋 다 사람이 봐야 한다.
    metric: horizon_findings
    max: 0
    consecutive_days: 3
    deadline: 2026-08-14
    # **2026-08-05가 정확히 이 형태였다** (고도화 4). 08-04 리포트에서 이 항목은 깨끗하게
    # 통과 중이었고, 바로 그 순간 전제(1분봉이 스케줄러 위상 안에 도착한다)가 이미 거짓이었다.
    # 그 하루를 놓친 대가가 다음 날 상위 봉 3~17% 손실이다.
    premise:
      metric: delivery_latency_p99_seconds
      max: 3.0
      summary: "봉 확정 경로 전체가 '1분봉이 경계 뒤 곧 도착한다'를 전제한다"

  - id: crash-count-measurable
    summary: "크래시 집계 exit-code 오판 수정(P0-3) — 0건인 날에도 집계가 되는지"
    registered: 2026-08-05
    # 2026-08-04에 `native_crashes.available=False`("Get-WinEvent 실패")가 났는데, 원인은
    # **크래시가 0건이었기 때문**이다 — 창 안에 이벤트가 하나도 없으면 Get-WinEvent가
    # 비종료 오류를 내고 powershell.exe가 1로 끝나는데, 파이썬이 그 종료 코드를 실패로
    # 읽었다. 즉 UI 크래시 격리가 처음 성공한 날 성공을 증명할 수치가 사라졌다.
    #
    # `ui-crash-isolation`이 판정을 채우려면 이 항목이 먼저 서 있어야 한다 — 그게 없으면
    # 매일 "판정 불가"다. **min 기준**이다: 1이면 쟀다, 0이면 못 쟀다.
    metric: native_crashes_measurable
    min: 1
    consecutive_days: 3
    deadline: 2026-08-14

  # ------------------------------------------------------ 고도화 5종 (2026-08-05)

  - id: daily-axes-measured
    summary: "리포트 대조축 5종이 매일 실제로 측정되는지(고도화 1·2·4) — 미측정이 0인지"
    registered: 2026-08-05
    # **이 등록부의 메타 항목이다.** 다른 항목이 "판정 불가"로 정체되는 근본 원인이 전부
    # 여기 모인다 — 2026-08-04에 크래시 집계가 조용히 사라져 `ui-crash-isolation`이 영원히
    # 0/3이었던 것이 그 사례다.
    #
    # 이 값이 0이 되려면 장후에 두 도구를 돌려야 한다:
    #   python scripts/verify_archive_volume.py --date <오늘>
    #   python scripts/run_vol_scorecard.py     --date <오늘>
    # 그리고 수집 세션이 ClockSkew·FeatureHealth 로그를 남겨야 한다(자동).
    metric: unmeasured_count
    max: 0
    consecutive_days: 3
    deadline: 2026-08-19

  - id: no-degenerate-features
    summary: "세션 내내 죽어 있는 피처가 없는지(고도화 3) — nan_ratio가 못 보는 축"
    # 2026-08-06 재등록 — **검출기 자체가 틀렸던 것**이 드러나 고친 날로 등록일을 옮긴다.
    #
    # 08-05 등록 시 주석에 이렇게 적어 뒀다: *"첫 며칠은 0이 아닐 가능성이 높다. 그게
    # 오탐이면 임계가 아니라 웜스타트가 고쳐져야 할 것이라는 뜻이므로 그대로 둔다."*
    # 예상은 반쯤 맞았다 — 오탐이 맞았지만 **원인은 웜스타트가 아니었다.**
    #
    # 08-06 퇴화 10건 중 9건이 `px_gap_open`(= log(당일 시가/전일 종가), **장중에 변할 수가
    # 없다**)과 `px_ema_cross_*`(sign, 값역 {-1,0,+1}) · `px_breakout_*`(대부분 0.0)이었다.
    # 정의상·성질상 상수인 피처를 상수라고 잡고 있었고, 그 상태로는 `max: 0`이 **구조적으로
    # 통과 불가**였다. 매일 ERROR가 찍히면 늑대소년이 되고, 그건 이 등록부가 가장 경계하는 실패다.
    #
    # 처방은 임계 완화가 아니라 **판정 대상 정정**이다(`features/px_core.INTRADAY_CONSTANT_OK`).
    # 검출력은 그대로다: 이 셋도 `always_nan`이면 잡히고, 연속값 피처의 0 고착
    # (`px_macd_h_5` — 이 등록 항목이 애초에 겨냥한 그 피처)도 그대로 잡힌다.
    registered: 2026-08-06
    metric: degenerate_feature_count
    max: 0
    consecutive_days: 3
    deadline: 2026-08-20

  # ------------------------------------------ 2026-08-05 장중 점검 P0/P1/P2

  - id: composer-bucket-completeness
    summary: "합성기 겹④(마지막 구성 1분봉 대기, P0-1) — 상위 Horizon 버킷이 안 잘리는지"
    registered: 2026-08-05
    # 2026-08-05 장중 실측: 시계를 맞춘 **바로 그날** 26건이 났다. 3분봉 18개 중 12개가
    # 2분짜리, 30분봉 2개 다 29분짜리. 거래량 결손 3m 16.9% · 5m 11.2% · 10m 4.8%.
    #
    # 원인은 스큐가 아니라 **1분봉이 시각이 아니라 다음 분의 첫 틱으로 확정된다**는 것이었다
    # (발행 지연 중앙값 0.655초 > 스케줄러 위상 0.5초, 69%가 초과). 전날까지는 로컬 시계가
    # 9.7초 느린 덕에 우연히 안 터졌을 뿐이다 — P0-1(시계 동기)이 그 쿠션을 걷어냈다.
    #
    # **`horizon-volume-identity`로는 이걸 못 잡는다.** 그쪽은 아카이브를 보므로 장 종료 후
    # `run_recompose.py`를 돌리면 0이 된다. 이 지표는 수집 세션의 로그를 세므로 재합성에
    # 지워지지 않는다 — 결과가 아니라 원인을 채점한다.
    metric: late_bar_drops
    max: 0
    consecutive_days: 3
    deadline: 2026-08-19
    # **이 수정이 성립하려면 참이어야 하는 것** (2026-08-05 2차, 고도화 4).
    # 겹④는 마지막 구성 1분봉을 최대 5초 기다린다. 회선 지연 초과분이 그 상한에 가까워지면
    # 대기가 자주 헛돌아 `ComposerFlushedIncomplete`로 빠지기 시작한다 — 결과 지표가
    # 나빠지기 **전에** 이 값이 먼저 움직인다.
    premise:
      metric: delivery_latency_p99_seconds
      max: 3.0
      summary: "겹④ 상한 5초의 60% — 여기 닿으면 유예를 재산정하거나 회선을 봐야 한다"

  # -------------------------------------- 2026-08-06 P0 3종 (호스트 재부팅 사고 대응)

  - id: archiver-restart-restore
    summary: "아카이버 재기동 복원(P0-1) — 재기동이 그날 오전치를 지우지 않는지"
    registered: 2026-08-06
    # 2026-08-06 실측: 10:03:49 호스트 재부팅 → 10:25 재기동. `flow_intraday`와 옵션체인
    # 3시리즈의 첫 행이 전부 **10:26~10:31**이었다 — 08:35~10:03의 111~116분이 통째로
    # 사라졌다. 원인은 두 아카이버가 메모리에 있는 것만으로 그날 파일을 `os.replace()`
    # 하는 것이었고, 재기동하면 그 메모리가 비어 있다.
    #
    # 이 계열들은 **소급 조회가 아예 없다**(장중 수급은 당일 누적만, 옵션 시세는 과거 조회
    # 경로 없음). 봉처럼 백필로 메울 수가 없어서, 다음 재기동 때 또 지워지면 그날도 영구
    # 소실이다. 그래서 3거래일이 아니라 5거래일을 본다 — 장중 재기동은 매일 나지 않으므로
    # 짧게 잡으면 "재기동이 없었던 날"만 세고 통과해 버린다.
    #
    # **이 지표가 0이어도 "재기동에서 살아남았다"는 증명은 아니다** — 재기동이 없던 날은
    # 그냥 조용하다. 그 한계를 알고도 등록하는 이유: 이 축이 없던 8/5·8/6에는 소실이
    # **일어나는 중에도** 리포트가 완벽하게 초록이었다. 조용함을 증거로 착각하지 않되,
    # 시끄러움은 반드시 잡히게 해 두는 것이 이 항목의 값어치다.
    metric: series_gap_findings
    max: 0
    consecutive_days: 5
    deadline: 2026-08-20

  - id: boot-recovery-armed
    summary: "부팅 자동 복구 무장(P0-2) — 수집 작업에 at-startup 트리거가 살아 있는지"
    registered: 2026-08-06
    # 2026-08-06 실측: 10:03:49 재부팅(이벤트 1074, RuntimeBroker.exe, "기타(계획되지 않음)"),
    # OS는 10:05:03에 복귀. **MESSIAH는 10:25에 사람이 손으로 띄울 때까지 죽어 있었다** —
    # 등록된 트리거가 평일 08:35 하나뿐이었기 때문이다. 1분봉 21개 영구 소실.
    #
    # 그날 `scripts/install_scheduled_tasks.ps1`로 부팅 트리거·재시작·StartWhenAvailable을
    # 붙였다. 이 항목이 보는 것은 **그 설정이 오늘도 살아 있는가**다.
    #
    # **min 기준**이다(1=무장, 0=안 됨). 설정은 코드가 아니라 OS 상태라 테스트로 못 잡는다 —
    # 누가 작업을 지우거나 다시 만들면 조용히 08-06 이전으로 돌아가고, 그 사실은 다음
    # 재부팅 때 관측 공백으로만 드러난다. 이 프로젝트가 반복한 "결선했다고 믿는데 안 붙어
    # 있음"의 OS판이다.
    #
    # 한계를 분명히 해 둔다: 이건 **무장 여부**이지 복구가 실제로 동작한다는 증명이 아니다.
    # 후자는 장중에 재부팅이 한 번 나야 알 수 있고, 그때 `longest_gap_minutes`가 답한다.
    metric: boot_recovery_armed
    min: 1
    consecutive_days: 3
    deadline: 2026-08-14

  - id: ev-features-measured
    summary: "EV 카테고리 승격(고도화 5) — 관문이 상위로 지목한 ev_*가 실제로 측정되는지"
    registered: 2026-08-12
    # **아직 승격 전이라 `registered`가 미래 날짜다.** `evaluate()`는 등록일 이후 리포트만
    # 채점하므로 그때까지 조용히 "검증 대기"로 남는다 — 승격 전 며칠을 매일 "재발"로 울리면
    # 늑대소년이 되고, 그건 이 등록부가 가장 경계하는 실패다.
    #
    # 승격 절차(전부 장후, R11):
    #   1. python scripts/run_recompose.py --symbol A05608      ← 손상된 상위 봉 복구가 먼저
    #   2. python scripts/run_model_sweep.py --feature-set v2026.08-ev
    #      (아카이브 손상일이 남아 있으면 session_guard가 거부한다)
    #   3. configs/instance.yaml 의 feature_set: "v2026.08-ev"
    #
    # 이 지표가 0이 되지 않으면 **승격이 조용히 안 먹은 것**이다. 2026-08-04 관문이 가장
    # 값어치 있다고 지목한 카테고리가 운영에 안 붙어 있는 상태가 계속되는 것이라, 기한을
    # 넘기면 그 자체가 회의 안건이 된다.
    metric: absent_watchlist_features
    max: 0
    consecutive_days: 3
    deadline: 2026-08-21

  # ================================================================ 2026-08-07
  #
  # 그날 목위클리 옵션체인이 하루 종일 비었다. 반나절 조사 끝의 결론은 **KRX 규정상
  # 미상장 — 정상**이었다(8월 둘째 목요일 8/13 = 코스피200옵션 월물 최종거래일이라 그날
  # 만기 목위클리는 상장되지 않는다). 그 규칙은 이미 `core/event_calendar.py`에 있었고
  # (`has_thursday_weekly`, 2026-07-10 마흐디 실측 이식) 테스트까지 있었는데, 수집 경로가
  # 그 정본을 안 물어봐서 22줄의 틀린 WARNING과 사고 오판이 났다.
  #
  # 아래 셋은 **예측**이다. 등록부 헤더의 "존재한다부터 적는다" 원칙 그대로 — 좋아진다가
  # 아니라 "이 날짜에 이 값이 이렇게 나온다"를 미리 적고 실측이 채점하게 한다.

  - id: thursday-weekly-listing-calendar
    summary: "목위클리 상장 캘린더 결선(P0-1·P0-2·P0-3) — 미상장일에 조용하고, 재개일에 실제로 들어오는지"
    registered: 2026-08-07
    # `series_gap_findings`를 그대로 쓴다. 이 지표는 이미 `archiver-restart-restore`가
    # 보고 있지만, 그쪽은 "재기동에서 살아남았나"를 묻고 이쪽은 "미상장일에 헛경보가
    # 안 나나"를 묻는다 — **같은 지표, 다른 예측**이다.
    #
    # 예측(실측이 채점한다):
    #   2026-08-07~13  weekly_thu 0행이 ⊘(정상)로 판정된다 → 이 값에 안 들어온다
    #   2026-08-13     regular(월물) 만기일 — regular 체인은 정상 수집된다
    #   2026-08-14     weekly_thu 재개, 만기 2026-08-20물이 들어온다
    #
    # **8/14가 진짜 검증이다.** 그날 안 들어오면 규정이 아니라 이 판정식이 틀린 것이고,
    # 그때는 `OptionChainSeriesMissing`(ERROR)이 3사이클째에 뜬다.
    #
    # 5거래일: 미상장 구간(8/7·10·11·12·13)이 정확히 5거래일이라, 그 구간을 통째로
    # 조용히 넘겨야 통과한다. 3일로 잡으면 구간 안에서 통과해 버려 재개일을 못 본다.
    metric: series_gap_findings
    max: 0
    consecutive_days: 5
    deadline: 2026-08-21

  - id: launch-window-refusal-not-counted
    summary: "기동 창 거절을 재기동·관측 공백으로 세지 않는지(P0-4)"
    registered: 2026-08-07
    # 2026-08-06에 붙인 at-startup 트리거가 2026-08-07 07:23에 **처음 발화**했고 가드가
    # 설계대로 거절했다. 그런데 `SessionStart`는 이미 찍힌 뒤라 리포트는 그것을 기동으로
    # 세어 `재기동 1회` + `관측 공백 73분(원인 불명)` + 전 계열 `머리 구멍 72~82분`을 찍었다.
    # 전부 오탐이고, 아래 `ui-restart-observability`(max 5)를 **재발로 뒤집을 값**이었다.
    #
    # 즉 이 항목은 다른 항목을 지키는 항목이다. 부팅 트리거가 08:30 이전에 발화하는 날에만
    # 의미가 있고 그건 매일 나지 않으므로, 조용한 날을 통과로 세는 한계를 안고 등록한다.
    metric: observation_gap_minutes_max
    max: 5
    consecutive_days: 3
    deadline: 2026-08-21

  - id: canonical-consumers-wired
    summary: "정본을 안 쓰는 소비자가 없는지(고도화 2) — 이 프로젝트가 네 번 반복한 실패 형태"
    registered: 2026-08-07
    # `ops/canonical_consumers.py`의 판정을 리포트의 `breaches`에 실었다. 테스트로만
    # 지키지 않는 이유는 **매일 읽히는 문서가 리포트 하나**이기 때문이다 — CI가 빨간 채로
    # 며칠 가는 상황에서 테스트는 아무도 안 본다.
    #
    # `breaches`는 넓은 그물이라 다른 사고 하나가 이 항목을 재발로 만들 수 있다. 그걸
    # 알면서 쓰는 이유: 이 판정은 **코드 구조**라 그날 데이터와 무관하게 0이어야 하고,
    # 0이 아닌 날은 어느 쪽이 원인이든 봐야 하는 날이다. 전용 지표가 필요해지면
    # (= 이 항목이 남의 사고로 두 번 이상 뒤집히면) 그때 `canonical_consumer_gaps`를 판다.
    metric: breaches
    max: 0
    consecutive_days: 3
    deadline: 2026-08-21

  # ================================================================ 2026-08-07 장후
  #
  # 그날 13:41에 UI 스모크 테스트가 운영 버스에 `sys.kill`을 발행했고, 그것이 수집
  # 프로세스를 죽여 1시간 54분이 유실됐다(소급 불가 계열 3종 영구 소실). 근본 원인은
  # 테스트가 아니라 **버스 계약**이다 — `subscribe()`가 모든 구독자에게 kill을 자동
  # 배달하면서 "핸들러가 그것을 견뎌야 한다"는 계약은 어디에도 없었다.
  #
  # 그리고 네 축(봉 연속성·거래량 대조·관측 공백·크래시)이 전부 초록인 채로 그 일이 났다.

  - id: truncation-is-visible
    summary: "**잘림**이 보이는가(P0-2·P0-4·고도화 1) — 구멍이 아니라 끊김을 재는 축"
    registered: 2026-08-07
    # 세션 커버리지 최솟값(%)의 **min 기준**이다. 이 등록부에서 "일어나야 하는 일"을 재는
    # 두 번째 항목이다(첫째는 `tick-collection-live`).
    #
    # 95%: 판정 창 420분 기준 21분. `bar_tail_gap_minutes`(20분)·계열 꼬리 구멍 하한(20분)·
    # `MISSING_MINUTES_LIMIT`(20분)과 같은 크기를 비율로 옮긴 값 — 네 곳이 같은 질문에
    # 답하므로 임계도 같아야 한다.
    #
    # **2026-08-07 자신은 이 기준을 못 넘는다**(틱 70% · 옵션체인 81~86% · 수급 73%).
    # 그게 맞다 — 그날은 실제로 잘렸고, 1분봉만 백필로 복구됐다. 채점은 08-10부터 시작된다.
    metric: series_coverage_pct_min
    min: 95
    consecutive_days: 3
    deadline: 2026-08-21

  # ================================================================ 2026-08-10
  #
  # 그날 아침 08:20(Messiah)·08:25(Messiah-G2) 트리거가 정확히 제 시각에 떴고, self-check도
  # PASS였고, 두 프로세스 모두 그 자리에서 종료했다:
  #
  #     [기동 창] 기동 창(08:30~15:35) 이전 08:20:27 — 정시 트리거(08:35)에 맡기고 뜨지 않는다
  #
  # `session_guard.LAUNCH_WINDOW_START`가 08:30에 하드코딩돼 있었고, 그 근거는 "정시 트리거
  # 08:35보다 5분 이르게"였다. 2026-08-08 11:48~12:00에 사람이 스케줄러 GUI로 트리거를
  # 08:20/08:25로 옮기면서(이벤트 140 아홉 번) 그 전제가 조용히 무효가 됐다. 같은 편집에서
  # **부팅 트리거도 함께 지워졌다** — 08-06에 붙인 것이 이틀 만에 사라졌고, 08-07·08-08의
  # 이벤트 118(due to system startup)이 그 전까지는 살아 있었음을 증명한다.
  #
  # 세 겹으로 조용했다: ① 거부 경로가 종료 코드 0이라 스케줄러 기록은 `LastTaskResult=0`
  # (성공) ② 트리거는 OS 상태고 기동 창은 코드라 테스트가 못 본다 ③ 실제로
  # `test_scheduled_start_time_is_inside_the_window`가 **있었고 통과하고 있었다** — 08:35라는
  # *기억된* 시각을 단언했지 *등록된* 시각을 읽지 않았기 때문이다.
  #
  # 그리고 그 아침 `boot_recovery` 항목은 이미 경고를 찍고 있었다. 아무도 못 봤다.
  # **경고만 하는 채널은 이미 한 번 실패한 채널이다.**

  - id: schedule-single-source
    summary: "등록 시각 단일 소스화(2026-08-10 P0) — 정본과 실제 스케줄러 등록이 계속 맞는지"
    registered: 2026-08-10
    # `configs/scheduled_tasks.json`을 정본으로 두고 등록(install_scheduled_tasks.ps1)과
    # 판정(session_guard 기동 창)이 둘 다 그것을 읽게 했다. 기동 창은 이제 "가장 이른 수집
    # 트리거 − 여유분"으로 파생되므로 **"정시 트리거가 자기 가드에 막힌다"는 조합 자체가
    # 구조적으로 불가능**하다.
    #
    # 남는 위험은 하나뿐이다: 정본과 실제 등록이 어긋나는 것 — 즉 08-08에 실제로 일어난 일.
    # 사람이 GUI를 열어 시각을 바꾸면 어느 파일에도 안 남는다. 그래서 매일 실측한다
    # (`ops/host_health.py` `check_schedule_drift`).
    #
    # **min 기준**이다(1=일치, 0=어긋남). `boot-recovery-armed`와 같은 계열이고, 같은 한계를
    # 갖는다 — 이건 "오늘 등록이 정본과 같다"이지 "내일 아침 실제로 뜬다"의 증명이 아니다.
    # 후자는 다음 거래일 08:20에만 알 수 있고, 그때 `observation_gap_minutes_max`가 답한다.
    metric: schedule_matches_registration
    min: 1
    # 5거래일 — 이 항목이 잡으려는 것은 코드 결함이 아니라 **사람의 손편집**이고, 그건 매일
    # 나지 않는다. 3일로 잡으면 "아무도 스케줄러를 안 만진 사흘"만 세고 통과한다.
    # 08-06의 부팅 트리거가 정확히 그렇게 이틀 만에 지워졌다.
    consecutive_days: 5
    deadline: 2026-08-21

  - id: morning-launch-actually-happens
    summary: "정시 기동이 실제로 일어나는가(2026-08-10 P0) — 08:20 트리거가 수집으로 이어지는지"
    registered: 2026-08-10
    # 위 항목이 "설정이 맞는가"라면 이 항목은 **"그래서 아침에 실제로 떴는가"**다. 둘을 나누는
    # 이유: 2026-08-10에 설정은 전부 정상으로 보였다(작업 존재·트리거 발화·self-check PASS).
    # 어긋난 것은 설정과 코드 사이였고, 그 결과는 **관측 공백**으로만 드러났다.
    #
    # `observation_gap_minutes_max`를 쓴다 — `ui-restart-observability`·
    # `launch-window-refusal-not-counted`가 이미 보는 지표지만, 이쪽은 **아침 머리쪽 공백**을
    # 겨냥한 다른 예측이다. 08-10은 08:20~08:58의 38분이 비었으므로 이 기준을 못 넘는다.
    # 그게 맞다 — 그날은 실제로 잃었다. 채점은 08-11부터다.
    metric: observation_gap_minutes_max
    max: 5
    consecutive_days: 3
    deadline: 2026-08-21

  - id: no-silent-process-death
    summary: "프로세스가 죽고 안 돌아온 날을 리포트가 말하는가(P0-3) — SessionEnd 마커"
    registered: 2026-08-07
    # `restarts`는 "몇 번 다시 떴나"를 센다. 2026-08-07엔 l1_daily가 13:41에 죽고 안
    # 돌아왔는데 `재기동 0회`였고 **그게 사실이었다** — 재기동이 없었던 것이 문제였는데
    # 그 축은 그것을 말할 수 없었다.
    #
    # `breaches`로 채점한다: 비정상 종료가 있으면 breach가 늘어난다. 넓은 그물이라 다른
    # 사고에도 반응하지만, 이 항목이 묻는 것은 "그날 조용히 지나갔나"이므로 그래도 맞다.
    metric: breaches
    max: 0
    consecutive_days: 3
    deadline: 2026-08-21
