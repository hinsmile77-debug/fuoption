

브랜치 병합/푸시
오늘 장 마감 후 일일점검 — 15:35 이후 무결성 리포트가 P0 수정(WS 다중 레코드)의 첫 검증이 됩니다.
단 오늘 L1은 수정 전 코드로 기동했으므로, 실제 검증은 내일 수집분부터입니다.



이 세션의 최종 위치
결함 5개를 걷어냈습니다: WS 레코드 유실 → 재생 시계 → 임계값 선택 2종 → 탐색 공간 → 죽은 피처 2종. 그 뒤 남은 것이 적중률 ~52%, 확신도와 무관이라는 측정값입니다.

이건 이 사슬에서 처음으로 버그가 아닌 진술입니다. 다음은 임계값·하이퍼파라미터·Horizon 수가 아니라:

피처 재설계 — 현재 121개가 전부 가격/거래량 파생입니다. 설계상 MS(시장구조)·FL(수급)·OP(옵션)·RG(매크로) 카테고리가 있는데 전부 미구현입니다.
방향 예측에 필요한 정보를 조사하고 해서 애초에 입력에 없는지 확정하고
만일 없다면 다음 두가지를 조사해서 보고해
1. 방향 예측에 필요한 정보를 조사해서 투입하는 작업을 하는 경우의 손익 조사
2. 1항 조사와 더불어 적중률 50%의 벽이 방향에만 있는 문제인가 딥다이브
3. 예측 대상 전환 — 방향 대신 변동성/레인지 도입의 손익을 조사
