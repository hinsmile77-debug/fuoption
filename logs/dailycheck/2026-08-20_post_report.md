# MESSIAH 일일 점검 — 2026-08-20 / 장후

- 점검 시각: 16:05 KST (예약 실행 · 국면·날짜는 확정값으로 받음)
- 대상 국면: post
- HEAD `704bd4c` · 장후 배치 실행 sha `39728e9` · 장중 프로세스 실행 sha `50eff6c` · 미커밋 실제변경 **2파일**
- 증거: `logs/dailycheck/evidence_20260820_post.md`
- 선행 보고서: `2026-08-20_pre_report.md`(08:45) · `2026-08-20_intra_report.md`(12:25) · `2026-08-20_intra_report_1236.md`(12:36) · `2026-08-20_synthesis_report.md`(15:31)

---

## 0. 한 줄 결론

**하루는 설계대로 흘렀다 — 배치 6/6 완주 · ERROR 1건(등록부) · 데이터 유실 0 · 종료 시퀀스 전량 정상.
오늘의 수확은 사고가 아니라 「미확정으로 남겨 둔 것 세 개가 동시에 닫혔다」는 것이다**:
W-11(`px_max_ret_60` 상수의 정체)이 **세션 경계 봉 오염**으로 확정됐고, C-5(meta 확률 얼어붙음)는
**정상 쪽으로 크게 기울었으며**, C-6(발행 오프셋 악화의 기전)은 **계측 자체가 링버퍼라 영원히 판정
불가**임이 드러났다.

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| 장전 | 정상 | 자가점검 2회 전량 `[OK ]` · self-check PASS · `schedule_drift` 정본 일치 · 기동 지연 0.3분 |
| 장중 | 정상 | ERROR 0 · 10분 이상 공백 0 · `late_bar_drops` 0 · 거래량 항등식 0.9998(유실 32계약/151,655) |
| 장후 | **조건부** | 배치 6/6 완주 · 산출물 7종 전량 존재. 그러나 등록부 재발 1건(1-1)과 계측 절단(1-2) |

**P0: 1건** — `no-degenerate-features` 3번째 재발(1-1). 등록부 규율상 P0 자리에 둔다.
다만 **오늘 실거래 노출은 없다**(dev/simulator · `DecisionEmitted` 14건 전량 `NO_TRADE` · 주문 0건 ·
`meta_gate.passes = 0`). 사람의 즉시 개입이 필요한 항목은 없고, 아래 Fix는 전부 오늘 장후~내일 장전 적용분이다.

---

## 1. 이상점 정리보고

### P0 — 오늘 거래에 직접 위험

#### 1-1. 세션 경계 봉이 「단봉 수익률」로 계산된다 — `no-degenerate-features` 3번째 재발의 진짜 원인 (**W-11 종결**)

- **증상**: `px_max_ret_60`이 10m Horizon에서 세션 내내(41봉) 단 하나의 값에 고정.
  등록부가 "수정이 듣지 않았다"로 3번째 재발 판정.

- **근거**:
  ```
  15:35:06 [WARNING] FeatureHealthDegenerate  10m 피처 1개가 세션 내내 죽어 있었다
                      — 항상NaN [] · 상수 ['px_max_ret_60']   (samples=41, min_samples=30, judged=true)
  15:45:38 [ERROR]   FixVerificationRecurred  no-degenerate-features: 오늘(2026-08-20) 기준 위반
                      — 수정이 듣지 않았다 (최초 위반 2026-08-13 이후 3회)
  ```
  `logs/l1_daily_20260820.log` · `logs/postmarket_20260820.log`

  **아카이브에서 직접 재현했다** (`data/bars/A05609/10m/*.parquet` 4일치 153봉, 오늘 42봉 평가):

  ```
  px_max_ret_60 distinct values on 2026-08-20: 1 of 42   (값 +0.032462281826249884)

  TOP5 단봉수익률 (60봉 창 안):
    +0.032462  2026-08-19 15:30 KST -> 2026-08-20 08:40 KST   ← 창의 argmax (17시간 간격)
    +0.019337  2026-08-14 15:30 KST -> 2026-08-17 08:40 KST   ← 주말 갭 (3일 간격)
    +0.017546  2026-08-20 09:00     -> 2026-08-20 09:10       ← 오늘 장중 최대
  ```

  `px_max_ret(bars, 60)`은 `src/messiah/features/px_core.py:192` — 인접 종가비 최댓값이다.
  **창 60은 「60분」이 아니라 「60봉」**이고, 10m에서 60봉 = 600분 ≈ **1.46 거래일**이다.
  한 세션(410분 ≈ 41봉)이 창보다 짧으므로 argmax가 세션 중에 창을 벗어날 수 없고,
  그 argmax가 하필 **전일 종가 → 당일 시가**를 잇는 세션 경계 봉이다.
  야간 갭 +3.25%는 오늘 장중 최대 수익률(+1.75%)의 **1.85배** — 어떤 장중 봉도 이걸 못 넘는다.

- **기준 위반**:
  - SYSTEM.md **R16** — *"Feature 계산은 known-value 회귀 테스트 필수."* 세션 경계를 포함한
    다중일 버퍼에 대한 known-value 케이스가 없다.
  - SYSTEM.md **R10** — 세션 경계 봉은 실제 17시간 간격을 10분봉 한 칸으로 접은 **합성 관측치**다.
    배지도 경보도 없이 피처 입력에 들어간다.
  - dev_memory `NEXT_TODO.md:3868` **W-11** — *"`px_max_ret_60`이 10m에서만 상수인 이유 — 피처 정의
    창 60분과 10m 40표본(창 6봉)의 관계."* **이 가설은 틀렸다.** 창은 60봉이지 60분이 아니고,
    상수의 원인은 표본 수가 아니라 세션 경계 봉이다.

- **영향**: `px_max_ret_*`만의 문제가 아니다. **인접 종가 차분을 쓰는 모든 피처**
  (`px_ret_*` · `px_mom_*` · `px_accel_*` · `px_kurt_r_*` · `px_skew_r_*` · `vl_rv_*` ·
  `vl_semi_up/dn_*` · `vl_vov_*`)가 롤링 창이 세션 경계를 넘는 순간 **17시간짜리 관측치 하나를
  10분봉으로 취급**한다. 평균·표준편차 계열은 이상치가 희석돼 값이 계속 변하므로 계기에 안 걸리고,
  **max/min 계열만 이상치에 고정(pin)돼 눈에 보인다.** 즉 `px_max_ret_60`은 병이 아니라 **유일한 증상**이다.
  현재 라이브 번들 `real-20260811-1604-30m`이 이 오염된 입력 위에서 학습·추론 중이다.

- **신규 여부**: **재발**(3회 · 최초 2026-08-13). 원인은 **오늘 최초 규명** — dev_memory에는
  `DECISION_LOG.md:5114`에 *"창 길이인지 버그인지 미확정 · 조사 먼저(W-11)"* 로만 남아 있었다.
  **둘 다다** — 창 길이가 세션보다 길어서, 세션 경계 버그가 드러난 것이다.

---

### P1 — 정확성·관측 훼손

#### 1-2. `delivery_latency`가 세션 전체가 아니라 **끝 20,000 프레임**이다 — 주석은 "세션 전체"라고 말한다

- **증상**: 하루 대표 지연 지표의 표본 수가 정확히 상한값 `20000.0`. 절단이 일어났는데
  리포트도 로그도 그 사실을 말하지 않는다.

- **근거**:
  ```
  15:35:06 [INFO] TickDeliveryLatency  회선 수신 지연 상한 — p50 0.512s · p90 0.921s · p99 1.025s
                   · 최대 1.143s (표본 20000건, frac(t)만큼 과대평가된 상한)
  ```
  같은 세션의 `TickArchiveSummary`는 **137,977행** 적재를 보고한다.

  ```python
  # src/messiah/ops/clock_skew.py:109
  self._latencies: deque[float] = deque(maxlen=latency_capacity)   # _LATENCY_CAPACITY = 20000
  ```
  그런데 같은 클래스 `observe()`의 docstring(:117)은
  *"같은 자리에서 수신 지연 상한(ŝ − 표본)도 **세션 전체 목록**에 쌓는다"* 라고 적혀 있다.
  `deque(maxlen=...)`은 세션 전체가 아니라 **끝에서 20,000개**만 남긴다.

- **기준 위반**: SYSTEM.md **R10**(계측의 절단은 배지·경보 동반) · **L18**(0과 "모른다"의 구분 —
  여기서는 "하루 값"과 "끝 한 토막 값"의 구분). `references/phases.md` D절 *"건수 0은 두 가지다"* 의
  같은 계열: **표본 20,000은 「20,000건 봤다」가 아니라 「20,000건까지만 기억한다」다.**

- **영향**: 세 가지가 동시에 무너진다.
  1. **오늘의 J-10/L-7 판정 기반**. p90 921.4ms로 *"1,000ms 미만 → G-2 재착수 조건 미충족"* 을
     냈는데, 이 921ms는 하루가 아니라 세션 끝 토막의 값이다.
  2. **C-6(발행 오프셋 악화가 회선인가 프로세스 내부인가)이 구조적으로 판정 불가.**
     12:36 리포트가 L-8에 *"시간대별로 수동 재계산"* 을 걸었지만, **원자료가 링버퍼에 덮여
     남아 있지 않다.** 손으로도 못 한다.
  3. **`MinuteBarAggregator.flush_due()`의 유예 근거**(같은 docstring이 명시한 소비처)가
     편향된 분포를 받는다.

- **신규 여부**: **신규.** dev_memory에 `delivery_latency` 항목은 여럿 있으나
  표본 절단을 지적한 항목은 없다.

#### 1-3. 국면 어긋남 3번째 사례 — Δt 가설이 **역전됐다가 완전히 무너졌다**, off-by-one 확정

- **증상**: `RegimeClassified`의 국면과, 같은 사이클 `AggregatorNoContribution.regime`이 하루 **3회** 불일치.
  12:36 리포트가 세운 *"어긋남의 Δt가 모든 일치보다 크다"* 는 오후에 반증됐다.

- **근거** (`logs/g2_daily_20260820.log` 14사이클 전량 대조):

  | 시각 | `RegimeClassified` | 집계기가 쓴 국면 | Δt | 판정 |
  |---|---|---|---|---|
  | 11:00 | TREND_UP | **RANGE**(=10:30분) | 117ms | ✗ |
  | 12:30 | TREND_DOWN | **RANGE**(=12:00분) | 334ms | ✗ |
  | **13:00** | RANGE | **TREND_DOWN**(=12:30분) | **72ms** | ✗ ← **신규** |
  | 일치 11건 | — | — | 29~243ms | ✓ |

  ```
  13:00:00.831 RegimeClassified          regime=RANGE      confidence=0.9821
  13:00:00.903 MetaGateEvaluated         probability=0.024909817679844813
  13:00:00.903 AggregatorNoContribution  regime=TREND_DOWN   ← 직전 12:30분 국면
  ```

- **기준 위반**: dev_memory 기존 항목 **F-C(봉 동일성 판정)** 의 설계 근거. `DECISION_LOG` 12:36분
  항목이 *"경합의 모양이 아니라 한 사이클 밀림(off-by-one bar)이다"* 로 이미 결론냈고, 오늘 오후가 확증했다.

- **영향**: 두 가지가 확정된다.
  1. **Δt는 판별력이 전혀 없다 — 확정.** 13:00의 어긋남 Δt 72ms는 일치 사이클 11건 중 **9건보다 작다**.
     12:36 리포트의 *"어긋남 335ms > 모든 일치"* 는 표본 2건짜리 우연이었다. **경합(race) 가설 완전 기각.**
  2. **off-by-one 규칙성은 유지된다 — 강화된다.** 어긋남 3건 **전부** 정확히 직전 사이클의 국면을 썼다.
     그리고 3건 **전부** 국면이 바뀐 사이클이다(전환 6회 중 3회 어긋남 · 전환 아닌 8회는 전량 일치).
     즉 **"전환에서만 난다"는 참, "전환이면 항상 난다"는 거짓** — 조건부 밀림이다.

- **신규 여부**: 기존 **F-C**의 세 번째 증거 겸 **판정 축 확정**. 새 항목을 만들지 않는다.
  다만 **12:36 리포트의 서술은 정정 대상**이다(아래 §5).

#### 1-4. 1분봉 발행 오프셋이 09시 75ms → 14시 885ms — 일별 p90 한 숫자로는 안 보인다 (L-9 예측 빗나감)

- **증상**: `FeaturePublish`(1m)의 분 경계 대비 오프셋이 오전 대비 오후에 **11.8배**.
  단, 12:36 리포트의 예측(*"14~15시 1.5초 이상"*)은 **빗나갔다** — 14시에 포화한다.

- **근거** (`logs/l1_daily_20260820.log` · 1m `FeaturePublish` 409건 시간대별 재계산):
  ```
  09시 n=59  p50=  75ms  p90= 380ms  max= 962ms
  10시 n=60  p50= 140ms  p90= 412ms  max= 807ms
  11시 n=60  p50= 335ms  p90=1652ms  max=3151ms
  12시 n=60  p50= 664ms  p90=1863ms  max=3526ms
  13시 n=60  p50= 653ms  p90=1306ms  max=2215ms
  14시 n=60  p50= 885ms  p90=2115ms  max=4232ms   ← p50 최악
  15시 n=35  p50= 789ms  p90=1701ms  max=2360ms   ← 꺾임
  ```
  같은 날 일별 단일값은 `daily_integrity_20260820.json` `delivery_latency.p90 = 0.9214s` 하나뿐이다.

- **기준 위반**: 설계상 1분봉은 거래소 시각 경계 구동(`bar_close: timer`)이며,
  기동 자가점검이 이미 경고했다 — *"유예 500ms vs 전일 회선 p90 925ms(2026-08-19) —
  완성봉이 늦은 틱을 놓칠 수 있다."* 오늘 11시 이후 p90은 그 유예 500ms를 **매 시간 초과**했다.

- **영향**:
  - **오늘은 실손해 0.** `AggregatorLateTickDropped` **0건** · `late_bar_drops: 0` ·
    거래량 항등식 0.99979(누락 32계약/151,655) · `missing_minutes: 0`. 유예 500ms를 넘겨도
    합성기가 봉을 놓치지 않았다는 뜻이다.
  - **그러나 마진이 사라졌다.** 14시 max 4,232ms는 유예의 8.5배다. 오늘은 운이 좋았다.
  - **L-9 예측 채점: 빗나감.** 09→12시 단조 증가(75→664ms)를 선형 연장해 1.5초를 예상했으나
    실측은 13시부터 **포화**(653→885→789ms). 악화는 선형이 아니라 **오전 1시간의 계단 뒤 고원**이다.
    이 형태는 "회선이 갈수록 나빠진다"보다 **"11시경 무언가가 한 번 바뀌고 그대로 유지된다"** 에 가깝다.
    (11시대는 `px` 60봉 창이 처음 채워지는 구간이기도 하다 — 1-1과 같은 시각축. 확정 아님, §확인 필요 C-6′)

- **신규 여부**: 12:36 리포트 1-1의 연장이며 **F-E** 항목이 이미 서 있다. 오늘 추가된 것은
  ① 오후 실측값 ② **예측 형태의 반증**(선형 아님, 포화) ③ 실손해 0 확인.

---

### P2 — 운영 부담·기술부채

#### 1-5. 미커밋 실제 변경 2파일이 실전 PC에 남아 있다 — 내일 08:20 기동이 이 코드를 태운다

- **증상**: 개행(CRLF) 잡음을 제외한 실제 코드 변경이 커밋되지 않은 채 남아 있다.

- **근거**:
  ```
  $ git diff --ignore-all-space --numstat -- src scripts
  scripts/run_g2_paper_trading.py   ±66
  scripts/run_l1_daily.py           ±71
  ```
  내용은 **F-D**(기동 창 판정을 `main()`에서 `__main__`으로 올려 Docker·self_check 헛돔 제거)다.
  HEAD `704bd4c`(16:01)까지 3커밋이 있었으나 이 두 파일은 포함되지 않았다.

- **기준 위반**: 금지 15계명 **10**(미커밋 수정 실전 반입 금지).

- **영향**: 내일 08:20/08:25 정시 트리거는 **커밋되지 않은 코드**로 뜬다. 그러면
  `session_git_shas`가 가리키는 sha와 실제로 돈 코드가 어긋나고, 내일 로그는
  "어느 코드의 결과인지" 말할 수 없게 된다 — 오늘 세운 fix들의 검증이 통째로 흐려진다.

- **신규 여부**: 신규(오늘 F-D 작업분).

#### 1-6. `loss_breakdown.series_head_gap_minutes`(10.0)와 `irrecoverable_loss_minutes`(0.3)가 같은 이름으로 다른 것을 말한다

- **증상**: 분해값의 최대 항이 10.0인데 총합은 0.3이다. 사람이 읽으면 "10분 잃었는데 0.3분으로 적혔다"로 오독한다.

- **근거**:
  ```json
  "irrecoverable_loss_minutes": 0.3,
  "irrecoverable_loss_breakdown": {
    "start_lag_minutes": 0.3, "series_head_gap_minutes": 10.0, "mid_session_gap_minutes": 0.0 }
  ```
  `src/messiah/ops/integrity_report.py:2172`가 `max(irrecoverable_heads)` **차감 전 원값**을 싣고,
  같은 함수의 `irrecoverable_loss_minutes()`(:1221)는 `head_gap − cadence`(10.0 − 10.0 = 0)를 쓴다.
  **동작은 설계대로다**(2026-08-18 F-0818P-5, *"카덴스는 손실이 아니다"*). 표기만 어긋난다.

- **기준 위반**: SYSTEM.md **R6** 정신(하나의 이름 = 하나의 뜻)의 데이터 판. 조항 직접 위반은 아니다 → P2.

- **영향**: 오독 위험만. 오늘 예산 경보(5거래일 44분 > 20분)의 근거는 08-14의 33분이지 오늘이 아니다.

- **신규 여부**: 신규(표기 축). 예산 초과 경보 자체는 기존 항목 — **중복 보고하지 않는다.**

---

### 확인 필요 (확정 아님)

- **C-5 — 「정상」 쪽으로 크게 기울었다.** 12:36에 3연속이던 `MetaGateEvaluated.probability`
  `0.024909817679844813` 런은 **4연속(11:30·12:00·12:30·13:00)에서 끝났고**, 이후 값이 크게 흩어졌다:
  `13:30 0.0201 → 14:00 0.0802 → 14:30 0.6672 → 15:00 0.2418 → 15:30 0.5600`.
  12:36의 판정선은 *"6연속 이상이면 고장 쪽으로 크게 기운다"* 였다 — **미달.**
  얼어붙은 값이 아니라 **GBDT 같은 잎에 4번 떨어졌다가 벗어난 모양**이다.
  **닫으려면**: F-F(`meta_features_digest`) 적용 후 08-21 N-2 — 확률이 같은 사이클에서 digest도 같은가.
- **C-6 — 판정 불가로 확정.** 1-2 때문에 시간대별 회선 지연 재계산이 **원자료 부재로 불가능**하다.
  L-8은 "손으로 하면 된다"가 전제였는데 그 전제가 깨졌다. **F-G(1-2 fix) 없이는 영구 미결.**
- **C-6′ (신규)** — 발행 오프셋 계단이 11시대에 서는 것과, `px_*_60` 계열 60봉 창이 그 무렵
  처음 채워지는 것이 **같은 시각축**이다. 우연인지 인과인지 모른다.
  **닫으려면**: F-E 적용 후 시간대별 오프셋을 Horizon별로 분해 — 1m만 나빠지면 회선/버스,
  전 Horizon이 같이 나빠지면 피처 계산 비용(60봉 창 충전 후 연산량 증가)이다.
- **L-5(F-5 국면 시드)** — `regime_unseeded_cycles: 0`이나, g2가 `50eff6c`(F-5 커밋 `05bcbd2` 09:13 이전)로
  08:25에 떴으므로 **오늘 값은 설계 채점이 아니다.** 장전 리포트의 보류를 유지한다.
  `RegimeWarmStart` 1건은 별개 태그다(08-20 K-3에서 확인 완료).

### 오늘 결론이 난 「확인 필요」 — 장후의 고유 수확

| 항목 | 출처 | 결론 |
|---|---|---|
| **W-11** | 08-19 장후 | **종결.** 창 60봉(=600분) > 세션(410분) + 세션 경계 봉 오염. 가설("창 60분")은 틀렸다 → 1-1 |
| **J-10 / L-7** | 장전 08:45 | `delivery_latency.p90 = 921.4ms < 1,000ms` → **G-2 재착수 조건 미충족.** 단 표본 편향으로 신뢰도 하락(1-2) |
| **L-9** | 장중 12:36 | **예측 빗나감.** 14시 p50 885ms(1.5초 아님) · `AggregatorLateTickDropped` **0건** → G-2 조건 재차 미충족 |
| **L-10** | 장중 12:36 | **F-C 설계 확증.** 어긋남 3건 전부 직전 사이클 국면 · Δt 판별력 완전 소멸 → 1-3 |
| **L-11 / C-5** | 장중 12:36 | 4연속에서 종료 · 이후 분산 → **「정상」 쪽.** 확정은 N-2 |
| **L-8 / C-6** | 장중 12:36 | **판정 불가 확정** — 원자료가 링버퍼에 덮였다 → 1-2 |
| **L-1** | 장전 | `abnormal_exits: []` **0건** — 08-19 F-1이 정상일에 조용하다 ✓ |
| **L-2** | 장전 | `incomplete_day: false` · `session_coverage_pct_min: 99.3` ≥ 95 — 08-19 F-3 정상일 채점 통과 ✓ |
| **L-3** | 장전 | 재발 **1건**(1-1) · 계측 성립·값 위반 0 · 계기 실명 0 — 08-19 F-4/G-4 부분 통과 |
| **L-4** | 장전 | `irrecoverable_loss_minutes: 0.3` — 0~5분 복귀 ✓ (5거래일 합 44분 경보는 08-14 지배) |
| **L-6** | 장전 | 목위클리 **만기일**. `option_calendar_violations` 0 · `ev_expiry_flag=1.0`·`ev_dte_opt_w=0.0`이 `allowed_constant_values`에 등재 → **오탐 없음** ✓ |
| **J-12** | 장전 | `SessionStart 2 − LaunchWindowRefused 1 − starts_by_process 1 = 0` → **오탐 없었다** ✓ |

### 이상 없음으로 통과시킨 체크리스트 (phases.md C절)

- **C-1 종료 시퀀스**: `l1_daily` 15:35:31 정상 · `g2_paper` 15:35:00 정상 · `postmarket` 15:45:38 정상 ·
  `shutdown_watchdog` 15:40:02 동작 · `task_exit_codes` 3작업 전량 `code 0` · `abnormal_exits` 0. ✓ (R13 · 계명 14)
- **C-2 장후 배치**: 6/6 완주 · `steps_failed: 0` · 21.1KB(중단 아님) · 순서 정본대로. ✓
- **C-3 산출물 정합**: 7종 전량 존재 · `volume_check` ratio 0.99979 `ok: true` ·
  `vol_scorecard` 3 Horizon 전부 `measurable: true` · `g2_daily_returns.jsonl`에 08-20 행 추가 ·
  `horizon_findings` 0 · `series_findings` 0 · `observation_gaps` 0 · `unmeasured` 0. ✓
- **C-4 수정 검증**: 등록부 23건 — 검증 완료 19 · 회복 중 2 · 재발 1 · 대기 1 · 판정 불가 0 · 기한 초과 0.
- **C-5 기록 의무**: 당일 커밋 11건 전량 `[MW0601]` 접두어 ✓ · dev_memory 양쪽 오늘 갱신됨 ✓ ·
  미커밋 2파일 잔존 ✗ → 1-5.

---

## 2. Fix 작업 구현계획

> **이 예약 실행은 보고까지만 했다. 코드 변경·커밋·재기동 일절 없음.**
> 사용자가 "구현해"라고 지시하면 커밋 ①부터 착수한다. 각 커밋 전 `pytest`(해당 범위) + replay — 금지계명 2.

### F-G. 세션 경계 봉을 피처 입력에서 끊는다 — P0 · 대응 1-1

- **원인 가설**: 다중일 롤링 봉 버퍼가 세션 경계를 **봉 간격과 같은 것**으로 취급한다.
  `px_max_ret`는 `zip(closes, closes[1:])`로 인접쌍을 만들 뿐, 두 봉이 10분 떨어졌는지 17시간
  떨어졌는지 구분할 수단이 없다. `Bar`에 `session` 컬럼이 있는데(`data/bars/*.parquet` 확인)
  피처 계산이 그 컬럼을 안 읽는다.

- **변경 파일**:
  - `src/messiah/features/px_core.py`
    - 신설 `def _adjacent_returns(bars: Bars) -> list[float]` — `bar_open_kst`의 **날짜가 바뀌는
      쌍은 제외**하고 인접 수익률을 만든다. 반환 길이가 `len(bars)-1`이 아닐 수 있음을 docstring에 명기.
    - `px_max_ret`(:192) · `px_ret` · `px_mom` · `px_accel` · `px_kurt_r` · `px_skew_r` 의
      인접쌍 생성부를 전부 이 헬퍼로 교체.
  - `src/messiah/features/px_core.py`의 `vl_*` 계열 중 인접 종가비를 쓰는 것
    (`vl_rv` · `vl_semi_up` · `vl_semi_dn` · `vl_semi_ratio` · `vl_vov`) 동일 교체.
  - `src/messiah/features/engine.py` — `self._session`(`SessionState`)이 이미 세션을 알고 있다.
    버퍼에 봉을 넣을 때 세션 경계 플래그를 함께 실어 헬퍼가 날짜 파싱을 안 하게 한다(성능·정확성 양쪽).
  - `tests/features/test_px_core_session_boundary.py` **신설** — R16 known-value 회귀:
    08-19 15:30 → 08-20 08:40 봉쌍을 포함한 고정 입력에 대해 `px_max_ret_60`이
    **오늘 장중 최대 +0.017546**을 내야 한다(현재는 +0.032462).

- **회귀 위험**: **높다.** 라이브 번들 `real-20260811-1604-30m`은 **오염된 값 위에서 학습**됐다.
  피처 정의를 바꾸면 학습분포와 추론분포가 어긋난다. → **번들 재학습 없이 라이브 전환 금지.**
  당장은 `feature_set` 버전을 `v2026.08-ev` → `v2026.08-ev-sb`로 올려 **섀도로만 계산**하고,
  기존 값과 나란히 발행한다(R18 정신 — 계측 20거래일 후 승격).

- **검증 방법**:
  - `pytest tests/features/ -k "px_core or session"` — 신설 회귀 테스트 포함
  - replay: `src/messiah/simulator/replay.py`로 08-13·08-14·08-20(재발 3일) 재생 →
    `degenerate_feature_count`가 **3일 전부 0**이 되어야 한다
  - 다음 거래일 관측: `FeatureHealthDegenerate` 0건 · 등록부 `no-degenerate-features` **검증 완료** 복귀

- **적용 시점**: **오늘 장후(즉시 가능).** 단 섀도 발행으로만.

- **결정 필요 사항**: 세션 경계 쌍을 **제외**할 것인가, **갭 조정(gap-adjusted) 수익률**로 이어 붙일 것인가.
  **권고: 제외.** 갭 조정은 야간 정보를 살리려는 선택인데, 그 정보는 이미 `px_gap_open`이
  **전용 피처로** 들고 있다(`engine.py:591 prev_day_close_ticks`). 같은 정보를 두 경로로 넣으면
  `px_gap_open`과의 다중공선성이 생긴다.

### F-H. 지연 계측이 자기가 잘렸다는 것을 말한다 — P1 · 대응 1-2, C-6

- **원인 가설**: 링버퍼 `maxlen=20000`이 세션 후반만 남기는데, docstring은 "세션 전체"라 적혀 있어
  **아무도 절단을 의심하지 않았다.**

- **변경 파일**:
  - `src/messiah/ops/clock_skew.py`
    - `delivery_latency_seconds()`(:141) 반환에 `"truncated": bool` · `"capacity": int` ·
      `"observed_total": int` 3필드 추가. `observed_total`용 카운터를 `observe()`에 신설(deque 밖).
    - `observe()` docstring(:117)의 *"세션 전체 목록에 쌓는다"* 를 사실대로 정정.
    - **시간대 버킷 추가**: `self._latency_by_hour: dict[int, list[float]]` — 시(hour)별로
      p50/p90만 유지(전량 보관 아님). C-6을 영구히 답할 수 있게 하는 최소 구조.
  - `src/messiah/ops/integrity_report.py` — `delivery_latency`에 위 필드와 `by_hour` 전달.
  - `src/messiah/ops/fix_verification.py` — `delivery_latency`를 읽는 전제 지표가
    `truncated: true`면 **판정 불가(None)** 를 내도록. 0으로도 통과로도 세지 않는다(L18).

- **회귀 위험**: 낮다. 기존 p50/p90/p99/max 계산은 그대로 두고 필드만 추가한다.
  `fix_verification` 쪽은 오늘까지의 과거 리포트에 새 필드가 없으므로 `truncated` 부재 시
  기존 의미 보존(`False` 기본값) — 과거 판정을 소급해 뒤집지 않는다.

- **검증 방법**: `pytest tests/ops/ -k "clock_skew or latency"` /
  다음 거래일 `TickDeliveryLatency` 로그에 `truncated` · `by_hour` 출현 → **C-6 즉답**

- **적용 시점**: 오늘 장후. F-G와 별개 커밋.

- **결정 필요 사항**: `_LATENCY_CAPACITY`를 올릴 것인가. **권고: 올리지 않는다.**
  20,000 × 8바이트 = 160KB로 이미 충분히 크고, 138,000건 전량 보관은 1.1MB다.
  **문제는 용량이 아니라 침묵**이다 — 시간대 버킷이 있으면 전량 보관이 필요 없다.

### F-E(기존, 승격). 발행 오프셋을 시간대 축으로 남긴다 — P1 · 대응 1-4

12:36 리포트에 이미 선 항목. **오늘 관측으로 설계가 바뀐다**:

- 종전 계획은 "시간대별 p50"이었다. 오늘 실측이 **선형 악화가 아니라 11시 계단 + 고원**임을
  보였으므로, 기록 축에 **`step_detected`(전후 구간 p50 비율 ≥ 3.0인 경계 시각)** 를 함께 남긴다.
  단순 기울기(slope)는 오늘 형태를 못 잡는다 — 09→15시 회귀직선은 계단을 완만한 상승으로 뭉갠다.
- **변경 파일**: `src/messiah/ops/integrity_report.py` — `publish_offset` 축 신설 +
  `hourly_trend()` 공용 헬퍼(G-D와 같은 커밋). F-H의 `by_hour`와 **같은 자료구조를 쓴다.**
- **검증**: 오늘 로그 replay → `74/140/335/664/653/885/789ms` 재현 · `step_detected ≈ 11:00`

### F-D 커밋(미커밋분 정리) — P2 · 대응 1-5

- `scripts/run_g2_paper_trading.py` · `scripts/run_l1_daily.py`의 F-D(기동 창 판정 상향)를 커밋.
- **검증**: `pytest tests/ops/ -k session_guard` · 다음 부팅 트리거(06:4x)에
  `LaunchWindowRefused` 로그는 남되 Docker 기동·self_check 로그가 **없어야** 한다.
- **적용 시점**: 다른 무엇보다 **먼저**. 이게 안 되면 내일 로그가 어느 코드인지 모른다.

### F-F(기존). `MetaGateEvaluated`가 입력의 지문을 남긴다 — P2 · C-5 종결용

12:36 계획 그대로. `service.py:144`에 `meta_features_digest`(정렬 키 sha1[:8]) · `feature_as_of` 2필드.
**오늘 오후 관측으로 긴급도는 낮아졌다**(C-5가 정상 쪽으로 기움) — 순위를 F-E 뒤로 내린다.

### 적용 순서와 커밋 계획

| # | 커밋 | 포함 | 메시지 초안 |
|---|---|---|---|
| ① | F-D 정리 | 미커밋 2파일 | `[MW0601] 기동 창 판정을 self_check 앞으로 — 미커밋분 정리 (F-D)` |
| ② | **F-G** | px_core 세션 경계 + 회귀 테스트 + 섀도 feature_set | `[MW0601] 야간 갭이 10분봉 한 칸으로 들어가고 있었다 — 세션 경계 차단 (F-G·W-11 종결)` |
| ③ | F-H | clock_skew 절단 고지 + 시간대 버킷 | `[MW0601] 하루 지연이라 부른 숫자가 끝 한 토막이었다 — 절단 고지 + 시간대 축 (F-H·C-6)` |
| ④ | F-E + G-D | publish_offset 축 + hourly_trend/step_detected | `[MW0601] 계단은 기울기로 안 잡힌다 — 발행 오프셋 시간대 축 (F-E·G-D)` |
| ⑤ | F-F | meta digest | `[MW0601] 확률을 만든 입력의 지문 (F-F·C-5)` |

---

## 3. 고도화 방안

### G-F. 「세션 경계」를 피처 계약(feature contract)으로 승격한다

- **관측 근거**: 1-1. 오늘 아카이브 재현에서 상위 2개 단봉 수익률이 **둘 다 세션 경계 봉**이었다
  (+3.25% 야간 · +1.93% 주말). 장중 최대는 3위(+1.75%)다. 즉 **극단값 상위는 구조적으로
  경계 봉이 차지한다** — 우연이 아니라 매일 재현되는 성질이다.
- **제안 내용**: `src/messiah/features/spec.py`의 피처 명세에 `crosses_session: bool` 속성 신설.
  창 길이 × Horizon이 **한 세션(410분)을 넘는 모든 피처**를 기동 시 자동 열거하고,
  `_adjacent_returns` 계약을 지키지 않는 것이 있으면 **self-check에서 기동 거부**.
  오늘 기준 해당 피처: 10m의 `*_60`(600분) · 15m의 `*_60`(900분) · 30m의 `*_20`(600분)·`*_60`(1800분) —
  **`W_STD = (5, 20, 60)` 조합 18개 중 4개가 이미 경계를 넘는다.**
- **기대 효과**: 새 Horizon이나 새 창 길이를 추가할 때 같은 오염이 조용히 재발하는 것을 **기동 시점에** 막는다.
  측정 가능한 형태: self-check `feature_contract` 항목의 위반 수 = 0.
- **비용·위험**: 명세 속성 추가 + 자동 열거 로직. 반나절. 위험은 기동 거부가 너무 엄격해질 때인데,
  **처음 20거래일은 WARNING만**(R18) 하고 그 뒤 거부로 승격한다.
- **선행 조건**: F-G.
- **우선순위 제안**: 이번 주.

### G-G. 「계기가 자기 표본을 말한다」를 공용 규율로 만든다

- **관측 근거**: 1-2. `delivery_latency`가 표본 20,000으로 **상한에 정확히 닿았는데**
  리포트 어디에도 그 사실이 없었다. 그리고 그 침묵 때문에 L-8(수동 재계산)이
  **실행 불가능한 작업으로 하루 동안 TODO에 서 있었다.**
- **제안 내용**: `ops/`에 `Sampled` 공용 dataclass — `{value, samples, capacity, truncated, window}`.
  대상 소비처 4곳: `delivery_latency` · `clock_skew` · `nan_ratio_by_horizon` · `meta_gate`.
  `truncated: true`인 지표를 `fix_verification`이 읽으면 **자동으로 판정 불가(None)**.
- **기대 효과**: 이 저장소가 이미 세운 두 얼굴 — G-4(*"사건이 있었는데 0이면 눈이 멀었다"*) ·
  G-A(*"고쳤는데 새 경로가 안 쓰였다"*) — 에 이은 **네 번째 얼굴: "다 봤다고 말하지만 일부만 봤다".**
  (12:36이 세운 G-E *"값이 안 변한다"* 가 세 번째다.)
  얼어붙은 값은 침묵보다 나쁘고, **잘린 표본은 얼어붙은 값보다 나쁘다** — 값이 계속 변하므로
  살아 있어 보인다.
- **비용·위험**: 공용 타입 + 소비처 4곳 교체. 하루. 위험은 낮음(필드 추가형).
- **선행 조건**: F-H가 먼저 한 곳에서 형태를 잡는다.
- **우선순위 제안**: 다음 단계.

### G-H. 등록부 재발 판정에 「기전 미상」 상태를 만든다

- **관측 근거**: 1-1. `no-degenerate-features`는 08-13에 최초 위반 후 **엿새 동안 3회 재발**하는 내내
  *"수정이 듣지 않았다"* 라는 같은 문장을 냈다. 그런데 **애초에 수정이 없었다** —
  `DECISION_LOG.md:5114`가 *"창 길이인지 버그인지 미확정 · 조사 먼저(W-11)"* 로 적어 두었고,
  등록부는 그 사실을 모른 채 매일 ERROR를 냈다.
- **제안 내용**: `configs/pending_verifications.yaml`의 항목에 `fix_committed: <sha> | null` 필드.
  `null`이면 위반 시 `FixVerificationRecurred`(ERROR)가 아니라
  **`FixVerificationUndiagnosed`(WARNING)** 를 낸다 — *"고친 적이 없으므로 재발이 아니다"*.
- **기대 효과**: ERROR 1건의 의미가 회복된다. 오늘 장후 ERROR는 정확히 1건이었고 그것이
  이 항목이었는데, **"고친 게 안 듣는다"와 "아직 안 고쳤다"는 대응이 완전히 다르다**
  (전자는 원인 재조사, 후자는 그냥 착수). 측정: 등록부 `today_violating` 중
  `undiagnosed` 분리 계수.
- **비용·위험**: 등록부 스키마 1필드 + 판정 분기. 반나절.
  위험 — `fix_committed`를 사람이 안 채우면 전부 WARNING으로 내려앉아 경보가 둔해진다.
  → **기본값을 `null`이 아니라 「필수 입력」으로** 두고, 미기입 항목은 기동 self-check가 지적한다.
- **선행 조건**: 없음.
- **우선순위 제안**: 이번 주 (F-G와 같은 주 — F-G가 이 항목의 첫 `fix_committed`를 채운다).

### 로드맵 반영 제안

| 항목 | 현 로드맵 위치 | 제안 위치 | 사유 |
|---|---|---|---|
| F-G(세션 경계) | 없음(W-11 조사항목) | **즉시 · W-11 대체** | 조사 끝. 피처 정확성은 모델 학습 전 선결 조건 |
| G-F(피처 계약) | 없음 | 이번 주 | 새 Horizon 추가 시 재오염 방지 — 지금 4개가 이미 해당 |
| F-H·G-G(표본 고지) | 없음 | 오늘 장후 · 다음 단계 | C-6이 이것 없이는 영구 미결 |
| 번들 재학습 | Ver2.0 W단계 | **F-G 이후로 선행조건 추가** | 현 번들이 오염 입력으로 학습됨 |

---

## 4. 다음 거래일 관측 예정

| ID | 관측 대상(태그/지표) | 판정 기준 | 판정 예정일 |
|---|---|---|---|
| **P-1** | `FeatureHealthDegenerate` · 등록부 `no-degenerate-features` | F-G 적용 후 **0건** · 등록부 「검증 완료」 복귀. 08-13·08-14 replay도 0 | 08-21 장후 |
| **P-2** | `TickDeliveryLatency` | `truncated` · `capacity` · `observed_total` · `by_hour` 4필드 출현. `observed_total`이 `TickArchiveSummary.rows` 대비 몇 %인지 최초 관측 | 08-21 장후 |
| **P-3** | `delivery_latency.by_hour` | 09시 대비 14시 p90 비율. **≥ 3.0이면 회선 원인 · < 1.5면 프로세스 내부** → **C-6 종결** | 08-21 장후 |
| **P-4** | `publish_offset.step_detected`(F-E) | 오늘과 같은 11시대 계단이 **재현되는가**. 재현되면 구조, 안 되면 어제만의 사건 | 08-21 장후 |
| **P-5** | `session_git_shas` | F-D 커밋 후 기동이므로 HEAD와 **일치**해야 한다 → 1-5 마감 | 08-21 장전 |
| **N-2**(기존) | `meta_features_digest` | 확률이 같은 사이클에서 digest도 같은가 → **C-5 즉답** | 08-21 장후 |
| **C-6′** | `by_hour`를 Horizon별로 분해 | 1m만 악화면 회선/버스 · 전 Horizon 동반 악화면 피처 계산 비용(60봉 창 충전) | 08-22 |
| **L-4 연장** | `irrecoverable_loss_minutes` 5거래일 합 | 08-14(33분)가 창을 벗어나는 **08-21**에 44분 → 11분으로 떨어져 예산(20분) 이하 복귀하는가 | 08-21 장후 |

---

## 5. 재시동 권고

**재시동하지 않는다 — 재시동할 프로세스가 없다.**

| | 근거 |
|---|---|
| **재시동 없이 얻는 것** | 없음. `l1_daily` 15:35:31 · `g2_paper` 15:35:00 **이미 정상 종료**. `task_exit_codes`는 세 작업 전부 `code 0`. 보존할 프로세스 상태도, 끊길 관측 궤적도 없다 |
| **재시동으로 얻는 것** | 없음. 장중 프로세스는 종료됐고, 지금 띄우면 `session_guard.launch_window_verdict()`가 기동 창(08:15~15:35) 밖이라 **거절한다**(오늘 06:42 실측 2건) |

`code_version.stale = true`(15:34 스냅샷 · 프로세스 `50eff6c` vs 당시 HEAD `39728e9`)는 **오늘은 무해하다.**
그 시점 프로세스는 1분 뒤 정상 종료했고, 오늘 로그는 `session_git_shas: ["50eff6c"]` 단일 —
**어느 코드의 결과인지 말할 수 있다.** 오늘 관측은 온전하다.

**진짜 위험은 내일에 있다.** HEAD는 16:01 기준 `704bd4c`인데 **F-D 2파일이 미커밋**이다(1-5).
이 상태로 내일 08:20 정시 트리거가 뜨면 프로세스는 `704bd4c`를 자칭하면서 실제로는
그 코드 + 미커밋 F-D를 태운다 — **오늘 온전했던 "어느 코드인가"가 내일 깨진다.**

> **권고**: 재시동 대신 **커밋 ①(F-D)을 오늘 중 마친다.** 그러면 내일 08:20 정시 기동이
> 자동으로 정합한 코드를 태우고, F-G·F-H까지 커밋하면 **P-1~P-3이 내일 바로 채점된다.**

---

## 6. dev_memory 반영

- `DECISION_LOG.md` 추가 항목: `## [MW0601] 야간 갭이 10분봉 한 칸으로 들어가 있었다 — 2026-08-20 장후 점검`
- `NEXT_TODO.md` 추가 체크박스: **12건** (F-G·F-H·F-D커밋 · G-F·G-G·G-H · P-1~P-5 · C-6′)
- 완료·정정 처리한 기존 항목:
  - **W-11 종결** — 가설("창 60분 vs 40표본")은 틀렸다. 창은 60**봉**(600분)이고 원인은 세션 경계 봉이다
  - **L-1·L-2·L-4·L-6·L-7·J-10·J-12 통과 처리**
  - **L-8/C-6 → 판정 불가로 재분류**(F-H 없이는 영구 미결)
  - **L-9 예측 빗나감 기록** — 1.5초 예상 vs 885ms 실측, 형태도 선형이 아닌 계단+고원
  - **L-10/F-C 설계 확정** — replay 검증 케이스 2건 → **3건**(11:00·12:30·13:00)
  - **12:36 리포트 서술 정정** — *"어긋남의 Δt가 모든 일치보다 크다"* 는 **오후에 반증됐다**
    (13:00 어긋남 72ms < 일치 11건 중 9건). Δt는 **양방향으로** 판별력이 없다
  - **L-11/C-5 → 「정상」 쪽 기울기 기록**(4연속에서 종료 · 이후 분산)

---

*작성: messiah-daily-check 스킬 (예약 실행 · 장후) · **코드 변경·커밋·재기동 일절 없음** · 읽기(grep/sed/parquet 집계)와 문서 작성만 수행*
