# MESSIAH 일일 점검 — 2026-08-13 / 장중

> **관측 구간: 09:00 ~ 12:36 KST (3시간 36분).** 정규장은 15:35까지다 — **하루는 아직 끝나지 않았다.**
> 장후 산출물(`daily_integrity_20260813.json`·`self_eval`·`vol_scorecard`·`postmarket_*.log`),
> 종가 기반 지표(`delivery_latency` p99·`late_bar_drops`·`missing_minutes`), 15:35 이후에만 나오는
> 태그(`SessionEnd`)의 **부재는 결함이 아니라 "아직 차례가 아님"** 이다. 아래 §1.4에 따로 모았다.

- 점검 시각: 12:36 KST
- 대상 국면: `intra`
- HEAD `e37d387` · 실행 중 `e37d387` (`code_version.stale: false`) · 미커밋 179건
- 증거: `logs/dailycheck/evidence_20260813_intra.md`
- 직전 보고서: `logs/dailycheck/2026-08-13_pre_report.md` (동일 거래일 장전)

## 0. 한 줄 결론

**⚠ P0 없음.** 어제(08-12) P0였던 국면 마비는 **장중 시점에 이미 풀렸다**(UNKNOWN 100% → 12.5%, V-9 잠정 통과).
그 대신 그 fix가 가리고 있던 두 개가 드러났다 — **세션 첫 사이클 1건이 여전히 국면 없이 접히고**,
**나머지 7건이 `|S|=0.000`으로 접히는데 그것이 "우위 없음"인지 "입력 없음"인지 로그가 말하지 않는다.**

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| 장전 | 조건부(장전 보고서 참조) | 결함 ①(목위클리 재개일) · ②(유량 예산 축) 확정, 오늘 유지 |
| 장중 | **조건부 정상** | 파이프라인·데이터 연속성 완전 · 판단 사슬은 ④에서 전량 정지, 정지 사유가 관측 불가 |
| 장후 | 판정 불가 | 15:35 이후 — 관측 구간 밖 |

---

## 1. 이상점 정리보고

### P0 — 오늘 거래에 직접 위험

**해당 없음.** 확인 결과:

| 항목 | 값 | 출처 |
|---|---|---|
| `FixVerificationRecurred` | **0건** | l1·g2 로그 grep |
| `FixVerificationFailed` | **0건** | 〃 |
| `code_version.stale` | **false** — "코드 e37d387 — 전 프로세스 동일" | `logs/status_snapshot.json` 12:36:41 |
| `UnmatchedFill` | 0건 (불변원칙 5) | 〃 |
| `circuit_breaker` | `OK` · `phase: normal` · `gateway_halted: false` | 〃 |
| `irrecoverable_loss` | `clean: true` · `lost_items: 0` · 시작지연 0.6분 | 〃 |
| 컴포넌트 4종 | `l1.collector` / `l1.feature_engine` / `l1.composer` / `g2.pipeline` **전부 OK**, age 4.8~6.4초 | 〃 |
| 보유 포지션 | 없음 — `DecisionEmitted` 8건 전부 `NO_TRADE`, `OrderSubmitted` 0건 | `logs/g2_daily_20260813.log` |

**사람이 지금 개입해야 할 사유 없음.** 관망을 권고한다.

---

### P1 — 정확성·관측 훼손

#### 1-1. 세션 첫 판단이, 국면 판정이 이미 나온 뒤에도 국면 없이 접혔다

- **증상**: 09:00 사이클의 `DecisionEmitted`가 `② Regime=UNKNOWN`으로 접혔다. 그런데 같은 프로세스가
  **0.52초 앞서** `HIGH_VOL` 확신도 1.00을 이미 판정해 놓았다.

- **근거** (`logs/g2_daily_20260813.log`, 인접한 두 줄):
  ```json
  {"ts": "2026-08-13T09:00:00.851095+09:00", "tag": "RegimeClassified",
   "msg": "국면 판정 HIGH_VOL (확신도 1.00)", "regime": "HIGH_VOL",
   "confidence": 1.0, "bars_used": 200, "min_bars": 22, "rule_override": null}
  {"ts": "2026-08-13T09:00:01.367968+09:00", "tag": "DecisionEmitted",
   "msg": "② Regime=UNKNOWN — 이벤트/미판정 국면", "side": "NO_TRADE", "gate": "regime"}
  ```
  트리거가 된 피처는 그 사이도 아닌 **더 앞**이다 — `logs/l1_daily_20260813.log` 09:00:00.628567
  `FeaturePublish horizon=30m nan_ratio=0.0`.

  09:30 이후 7사이클은 `gate: "regime"`이 **한 번도 다시 나오지 않았다.** 첫 사이클 단건이다.

- **원인** (코드 확인, 변경 없음):
  - `src/messiah/strategy/futures/service.py:77` — `self._latest_regime = _UNSEEN_REGIME`
    (`:57` `RegimeState(regime=Regime.UNKNOWN, confidence=0.0)`)로 출발한다.
  - `:85 handle_regime()`이 버스에서 `RegimeState`를 **받아야만** 갱신된다.
  - `:88 handle_feature()` → `:111 _publish()` → `Aggregator.compute(..., self._latest_regime)` 경로가
    `RegimeState` 도착보다 먼저 돌면 초기 UNKNOWN이 그대로 집계에 들어간다.
  - `run_forever()`는 `feat.*`와 `intel.regime` **두 패턴을 한 구독으로 묶을 뿐 순서를 보장하지 않는다.**

- **기준 위반**:
  - 마스터플랜 Ver 2.0 §3.1 ② — 규칙 자체는 설계대로 동작했다. 위반은 **입력이 틀렸다**는 쪽이다.
  - SYSTEM.md **R6** — `_UNSEEN_REGIME`의 `UNKNOWN`이 *"아직 못 받았다"* 와 *"판정할 수 없다"* 두 뜻을
    겸한다. `phases.md` D절 「회색(UNKNOWN)은 정상이 아니다. 하나의 회색이 여러 뜻을 겸하고 있으면
    그것부터 분리 대상이다」에 정확히 해당한다.

- **영향**: 매 거래일 **세션 첫 30m 사이클 1건**이 결정적으로 `NO_TRADE`. 오늘 8건 중 1건(12.5%).
  종일로 환산하면 13건 중 1건(7.7%). 손익 영향은 작으나, 이 1건이 남아 있는 한
  `Regime=UNKNOWN 비율`이라는 채점 축이 **0에 수렴하지 못한다.**

- **신규 여부**: **신규.** 어제(08-12)는 14/14 전량이 UNKNOWN이라 이 잔여분이 관측될 수 없었다.
  어제 P0 결함 ①의 fix(`9170ce8` RegimeRuntime 웜스타트)가 8건 중 7건을 풀었고 **첫 사이클만 남겼다.**
  dev_memory `DECISION_LOG.md:4503` 항목의 **후속**이지 재발이 아니다.

---

#### 1-2. `|S|=0.000`이 7사이클 연속인데, 그것이 "우위 없음"인지 "입력 없음"인지 로그가 말하지 않는다

- **증상**: 09:30~12:30의 7건이 전부 같은 문장이다. 소수 **3자리까지 정확히 0**이다.

- **근거** (`logs/g2_daily_20260813.log`, 7건 전문 중 처음과 끝):
  ```json
  {"ts": "2026-08-13T09:30:00.852710+09:00", "tag": "DecisionEmitted",
   "msg": "④ |S|=0.000 < 0.2 — 우위 부족", "side": "NO_TRADE", "gate": "score"}
  {"ts": "2026-08-13T12:30:00.729950+09:00", "tag": "DecisionEmitted",
   "msg": "④ |S|=0.000 < 0.2 — 우위 부족", "side": "NO_TRADE", "gate": "score"}
  ```
  `gate` 분포 — `regime` 1건(09:00) · `score` 7건 · `kill`·`dispersion`·`pass` **각 0건**.

- **왜 확정할 수 없나** — 두 상태가 같은 문장으로 나온다:

  `src/messiah/strategy/futures/aggregator.py:185`
  ```python
  if total_weight <= 0:
      return FuturesView(score=0.0, agg_p_up=0.0, agg_p_down=0.0,
                         uncertainty=1.0,   # 기여 의견 없음 = 최대 보수
                         dispersion=0.0, n_experts=0, model_versions=[], top_features=[])
  ```
  이 값이 `src/messiah/strategy/decision/meta_decision.py:100`의 ③(`dispersion 0.0 > 0.25`?)을
  **무사통과**하고 `:106` ④에서 `|0.000| < 0.20`으로 접힌다.
  → **기여 전문가 0명이 "의견은 있으나 약하다"로 보고된다.**

  판별에 필요한 값이 어디에도 안 실린다:
  - `meta_decision.py:141 _no_trade()` — `mlog.log("DecisionEmitted", rationale, symbol=…, side=…, gate=…)`.
    `n_experts` · `score` · `dispersion` · `uncertainty` **전부 미기재**.
    (`GATE_PASS` 경로만 `rationale` 문자열에 `n_experts`를 담는데, 오늘은 그 경로에 0건 도달했다.)
  - `strategy/futures/service.py` 전 구간에 **`ExpertView`·`FuturesView` 로그 태그가 하나도 없다**
    (`grep -rn "ExpertView" … | grep mlog` → 0건). l1의 `FeaturePublish`와 g2의 `DecisionEmitted`
    사이 구간이 **통째로 미관측**이다.

- **기준 위반**:
  - SYSTEM.md **금지계명 12 (조용한 폴백 금지)** — `total_weight <= 0`은 "최대 보수 모드" 폴백인데
    배지도 경보도 없이 INFO로 지나간다. **R10**(폴백은 배지·경보 동반)의 로깅 측 대응물이다.
  - SYSTEM.md **R6** — 사유 1개(`④ 우위 부족`)가 서로 다른 두 상태를 겸한다.
  - `phases.md` D절 「건수 0은 두 가지다 — 진짜 없었거나, 계측이 없거나. 어느 쪽인지 로그로 구분한다」.

- **영향**: 어제 결함 ②(*"그 마비를 어떤 축도 재지 않았다"*)가 가리키던 문제가
  **②에서 ④로 한 칸 옮겨간 채 그대로다.** 어제 도입한 gate 계측(`9170ce8`)은 **갈래 이름은 세지만
  갈래 안의 값은 세지 않는다.** Risk Engine·Sizer·OrderGateway는 오늘도 `GATE_PASS` 0건이라
  **통째로 미검증**이고, 그 이유가 "시장에 우위가 없어서"인지 "모델 출력이 집계에 안 닿아서"인지
  현재 로그로는 영원히 알 수 없다.

- **확정된 것 / 확정 못한 것**:
  - **확정**: 판별에 필요한 값이 로그에 없다는 사실(= 관측 결함). 이것만으로 P1이다.
  - **미확정**: `n_experts`가 0인지 1인지. **F-1이 적용되면 다음 거래일 첫 사이클에 즉시 판정된다.**
  - 참고 정황(확정 아님): `live 번들 결선: ['30m'] (feature_set=v2026.08-ev)` — 전문가는 1명이다.
    1명짜리 앙상블이 3.5시간 7회 연속으로 `p_up − p_down`을 **소수 3자리까지 정확히 0**으로 내는 것은
    가능하되 흔치 않다. 정황일 뿐 근거가 아니므로 판정에 쓰지 않는다.

---

### P2 — 운영 부담·기술부채

#### 1-3. 점검 도구의 로그 공백 임계가 30m 구동 프로세스에 그대로 적용돼 8건 전량 오탐

- **증상**: 증거 다이제스트 §9 자동 적신호 12건 중 **8건**(항목 4~11)이 `g2_daily`의 30분 공백이다.
- **근거**: `logs/dailycheck/evidence_20260813_intra.md` §5 — `08:25→09:00`(35분), 이후 `09:00→09:30`
  … `12:00→12:30` 30분 × 7건.
- **오탐인 이유**: `g2_paper`는 **30m Horizon 구동**이다.
  `logs/g2_daily_20260813.log` 기동행 `live 번들 결선: ['30m']` · `국면 결선 — … 구동 30m`.
  `RegimeClassified`·`DecisionEmitted`가 `09:00:00 / 09:30:00 / … / 12:30:00` **정확히 정각·30분**에
  8쌍씩 찍혔다. 30분 침묵은 **설계 그대로**이며, 오히려 침묵이 깨지면 그때가 이상이다.
- **기준**: `phases.md` D절 「"로그에 없다"는 "안 일어났다"가 아니라 "관측되지 않았다"」의 반대편 —
  **도구가 "관측되지 않음"을 "이상"으로 승격시켰다.**
- **신규 여부**: 형태로는 **같은 오탐 4회째.** dev_memory 기록: `daily-axes-measured` ·
  `LaunchWindowRefused` · 08-12 postmarket `SessionEnd`, 그리고 이것. 장전 G-3(`since` 하한)과
  **같은 계열의 다른 축**(시간 하한이 아니라 주기 하한)이다.
- **영향**: 점검 신호의 신뢰도 하락. 적신호 12건 중 8건이 무의미하면 나머지 4건도 흘려보게 된다.

---

### 1-4. 판정 불가 — "아직 차례가 아님" (결함 아님)

관측 구간이 12:36에서 끝나므로 아래는 **결함으로 세지 않는다.**

| 항목 | 왜 지금 판정 불가인가 | 언제 판정되나 |
|---|---|---|
| `delivery_latency` p99 | `daily_integrity_*.json`에만 실린다 (`ops/fix_verification.py:400`) | 장후 배치 |
| `late_bar_drops` · `missing_minutes` (V-8) | 〃 | 장후 배치 |
| `SessionEnd` (l1·g2) | 15:35 종료 시퀀스 산출 | 15:35 이후 |
| `postmarket_20260813.log` / `SessionEnd` (U-6·V-7) | 15:45 배치 산출 | 장후 배치 |
| `regime_distribution` (V-10) · `self_eval` · `vol_scorecard` | 장후 산출물 | 장후 배치 |
| 아카이브 `expiry_date` (V-5) · 체인 거래가능성 (V-1) | 종가 확정 후 아카이브 | 장후 배치 |
| Risk / Sizer / OrderGateway 통과 건수 | `GATE_PASS` 0건 — **상류가 접혀 도달 자체를 안 했다.** 하류의 결함이 아니다 | 1-2 해소 후 |
| `OptionChainCalendarViolation` 종일 누적 (V-2) | 12:33 기준 **51건**, 5분 주기 × 250분 = 51 — 장전 예상 ~80건 궤도 그대로 | 15:35 |

---

### 1-5. 긍정 관측 — 기준을 통과한 것 (기록용)

이상점이 아니지만 **다음 점검의 출발점**이므로 남긴다.

- **★ V-9 잠정 통과 — 어제 P0가 실제로 풀렸다.**
  `DecisionEmitted` 중 `Regime=UNKNOWN` **1/8 = 12.5% < 50%**. 어제는 **14/14 = 100%**였다.
  `RegimeWarmStart` 1건(08:25:52, `bars: 200 ≥ min_bars: 22`) → `RegimeClassified` 8건이
  `bars_used: 200`으로 `HIGH_VOL`(09:00~11:00 5건) → `RANGE`(11:30~12:30 3건, 확신도 0.75→0.99→1.00).
  **상수가 아니라 분포다.** `9170ce8`의 라이브 검증이 장중에 이미 성립했다 (종일 확정은 15:35).

- **데이터 연속성 완전 — 결손 0.** 장중에 끊긴 것은 되메울 수 없으므로 산술로 확인했다:
  | Horizon | `FeaturePublish` 건수 | 기대치 (08:45:58~12:37:59 = 232분) |
  |---|---|---|
  | 1m | 233 | 232 + 1 ✓ |
  | 3m | 77 | ⌊232/3⌋ = 77 ✓ |
  | 5m | 46 | ⌊232/5⌋ = 46 ✓ |
  | 10m | 23 | ⌊232/10⌋ = 23 ✓ |
  | 15m | 15 | ⌊232/15⌋ = 15 ✓ |
  | 30m | 8 | ⌊232/30⌋ = 7~8 ✓ |

  `AggregatorLateTickDropped` **0건** · `nan_ratio: 0.0` 전건 · l1 로그 08:15~12:36 **10분 이상 공백 0건**.
  `l1.composer` = "합성봉 **169**개 · 거래량 항등식 일치(유실 0)" — 169 = 77+46+23+15+8 **정확히 일치**.
  `clock offset +2.036s`가 완성봉 유예 500ms를 넘지만, 늦은 봉 드롭이 0이므로
  `bar_close: timer(거래소 시각 경계 구동)`가 흡수하고 있다(최종 확정은 V-8, 장후).

- **수급 재시도 — 장전 관측 ③의 결론이 종일 성질은 아니었다.**
  `InvestorFlowPollRetried` 4건이 **08:36:02 · 08:41:03 · 08:46:03 · 08:51:02**에 몰려 있고,
  **08:51 이후 3시간 45분간 0건**이다. `data/flow_intraday/K2I/2026-08-13.parquet`이
  **12:40에 81.2KB**로 갱신 중(어제 종료 시각 15:34 기준 117.0KB — 경과 시간 대비 정상).
  → 장전이 적은 *"실패율 100%라 예산 여유가 0"*은 **08:36~08:51 4샘플의 성질**이었지 종일 성질이 아니다.
  **F-3(재시도 소진율 계측)은 유지하되 긴급도를 낮춘다.** 장전이 "상수를 또 올리지 않는다"고
  결정한 것은 결과적으로 옳았다.

- `OptionChainSeriesMissing` **0건** (V-3 잠정) — 아침에 받은 체인이 도중에 사라지지 않았다.
  「마스터파일 선등재」 가설이 아직 배제되지 않았음을 뜻하며, **장전 F-1의 착수 조건은 여전히 미충족**이다.

- 기동 자가점검 l1·g2 각 2회 · **비-OK 0행** · `self-check: PASS`.
  07:03 기동 시도는 `LaunchWindowRefused`(설계대로 거부) → 08:20/08:25 정시 트리거로 실제 기동 완결.
  `SessionStart.git_sha = e37d387` = HEAD.

---

## 2. Fix 작업 구현계획

> **⚠ 장중이다. 아래 어느 것도 오늘 적용하지 않는다** — SYSTEM.md **R11**(장중 학습 금지·장중 배포 금지) ·
> **금지계명 3·4**. 본 점검 과정에서 **코드 변경·커밋·배포·재기동을 일절 하지 않았다.**
> 전 항목 **적용 시점: 장후 15:35 이후**(프로세스 종료 확인 뒤).

### F-1. 판단 갈래 *안의 값*을 로그에 싣는다 — **P1** · 대응 이상점 1-2

- **원인 가설**: `9170ce8`가 도입한 `gate` 계측은 *어느 갈래로 접혔나*를 답한다. 오늘 필요한 것은
  *그 갈래가 무슨 값을 보고 접었나*이고, 그 필드가 `_no_trade()`에 없다.
- **변경 파일**:
  - `src/messiah/strategy/decision/meta_decision.py`
    - `_no_trade()` (`:141`) — `mlog.log(...)` 호출에 구조화 필드 추가:
      `n_experts=view.n_experts`, `score=round(view.score, 6)`, `dispersion=round(view.dispersion, 6)`,
      `uncertainty=round(view.uncertainty, 6)`, `model_version=intent.model_version`.
      **`rationale` 문자열은 건드리지 않는다** — 모듈 주석이 *"문구를 다듬는 순간 조용히 0이 된다"*고
      경계한 그 실수를 재현하지 않기 위해, 문자열이 아니라 필드를 늘린다.
    - `decide()`의 `GATE_PASS` 경로(`:130` `mlog.log`) — **같은 필드 집합으로 통일**.
      지금은 PASS만 `n_experts`를 `rationale` 문자열 안에 담고 있어 두 경로의 관측 스키마가 다르다.
- **회귀 위험**: 낮음. 로그 볼륨 증가는 30분당 1건 × 필드 5개 — 무시 가능.
  `ops/integrity_report.py`가 `DecisionEmitted`를 태그 이름으로만 세므로(`:663` 인근) 파싱 파손 없음.
- **검증**:
  - `pytest tests/ -k "meta_decision"` — 기존 `rationale` 단언이 그대로 통과해야 한다(문자열 불변).
  - 다음 거래일 09:00 `DecisionEmitted`에 `n_experts` 필드 존재. **그 값이 0이면 1-2는 "입력 없음"으로,
    1 이상이면 "진짜 우위 없음"으로 즉시 확정된다.**
- **적용 시점**: 장후. **F-2·F-3보다 먼저 넣는다** — 이것이 나머지의 판정 근거를 만든다.
- **결정 필요 사항**: 없음.

### F-2. `n_experts == 0`을 ④에서 분리한다 — **P1** · 대응 이상점 1-2

- **원인 가설**: `aggregator.py:185`의 "기여 의견 없음" 폴백이 `score=0.0 · dispersion=0.0`을 내고,
  그 값이 ③을 무사통과해 ④의 "우위 부족"으로 표기된다. 두 사실이 한 문장을 공유한다.
- **변경 파일**:
  - `src/messiah/strategy/decision/meta_decision.py`
    - 상수 추가: `GATE_NO_EXPERT = "no_expert"`, `DECISION_GATES` 튜플에 편입.
    - `decide()` — ①(kill) 다음, **②(regime) 앞**에 갈래 삽입:
      ```python
      if view.n_experts == 0:
          return self._no_trade(view, "⓪ 기여 전문가 0 — 집계 입력 없음", gate=GATE_NO_EXPERT)
      ```
    - 모듈 docstring의 「규칙 우선순위」 블록에 ⓪을 명기(마스터플랜 원문에 없는 항목이므로
      *"Ver 2.0 §3.1 원문에 없는 구현 측 선행 갈래 — 폴백 가시화(R10)"* 라고 출처를 분명히 적는다).
  - `src/messiah/ops/integrity_report.py` — `decision_funnel` 집계의 gate 목록에 `no_expert` 추가.
- **R18 저촉 여부 — 저촉하지 않는다**: 게이트·차단 로직 **신설이 아니다.** ⓪이 잡는 입력은 지금도
  ④가 전부 `NO_TRADE`로 접고 있어 **차단 결과가 동일**하다. 바뀌는 것은 표기뿐이다.
  차단 계층은 Meta-Labeler/Risk/KillSwitch **3개 고정** 그대로 유지된다.
- **회귀 위험**: `decision_funnel`을 소비하는 리포트·검증 등록부가 gate 집합을 하드코딩하고 있으면
  KeyError. 착수 전 `grep -rn "DECISION_GATES\|decision_funnel" src/ scripts/` 로 소비처 전수 확인.
- **검증**: `pytest tests/ -k "meta_decision or funnel"` + `n_experts=0` 뷰를 넣어 gate가
  `no_expert`로 나오는 단위 테스트 신설.
- **적용 시점**: 장후. **F-1과 같은 커밋에 묶는다** (F-1 없이 F-2만 넣으면 갈래는 갈라지되 값은 여전히 안 보인다).
- **결정 필요 사항**: ⓪의 위치. **권고 — ② 앞.** 국면 UNKNOWN과 입력 0은 다른 사실이고, 둘 다 참이면
  더 상류인 "입력 0"을 말해야 원인 추적이 한 단계 짧아진다.

### F-3. 세션 첫 사이클에 국면을 도달시킨다 — **P1** · 대응 이상점 1-1

- **원인 가설**: `service.py:77`의 초기 `_UNSEEN_REGIME`(UNKNOWN)이, `intel.regime` 첫 메시지가
  도착하기 전에 걸린 09:00 집계에 그대로 실렸다.
- **두 안과 권고**:
  - **(a) 보류안** — `_publish()`에서 `self._latest_regime is _UNSEEN_REGIME`이면 집계를 건너뛰고
    `WARNING` 1건(`RegimeNotYetReceived`)만 남긴다.
    → 첫 사이클이 `NO_TRADE` 대신 **무발행**이 된다. 판단 건수가 줄어 채점 분모가 흔들린다.
    마스터플랜 §3.2 *"침묵이 아니라 판단이다"* 와 정면으로 어긋난다.
  - **(b) 선발행안 ★ 권고** — `scripts/run_g2_paper_trading.py`의 `_build_regime_runtime()`이
    웜스타트 직후(오늘 08:25:52 시점) **초기 `RegimeState`를 버스에 1회 발행**한다.
    그 시점에 이미 200봉을 보유해 `min_bars: 22`를 넘으므로 **판정할 정보가 이미 있다.**
- **권고 이유**: (a)는 증상을 감추고 관측을 줄인다. (b)는 08:25에 이미 존재하는 정보를 흘려보내지
  않을 뿐이다. 웜스타트 로더가 이미 정본 한 벌이라 dev_memory가 네 번 지적한
  *"정본 아닌 소비자"* 를 새로 만들지 않는다.
- **변경 파일**:
  - `scripts/run_g2_paper_trading.py` — `_build_regime_runtime()` 반환 직후,
    `runtime.classify()` 1회 → 결과 `RegimeState`를 `bus.publish(TOPIC_REGIME, ...)`.
    `RegimeWarmStart` 로그 바로 뒤에 `RegimeSeeded` 태그 1건(INFO)을 남겨 **선발행이 있었음을 관측 가능하게** 한다.
  - `src/messiah/core/logging.py` — `"RegimeSeeded": logging.INFO` 등록.
- **회귀 위험**: 08:25 시점 국면이 09:00 개장 국면과 다를 수 있다(전일 종가 기반 200봉).
  다만 09:00:00.851의 정규 판정이 **0.5초 안에 덮으므로** 노출 창이 극히 짧고, 오늘 09:00 판정이
  `HIGH_VOL 확신도 1.00`이었던 점을 보면 이 창에서 잘못된 방향으로 진입할 확률은 낮다.
  그럼에도 **첫 사이클이 곧바로 `GATE_PASS`로 빠지는 것을 원치 않으면**, F-2의 ⓪처럼
  `state_duration_bars == 0`인 시드 상태를 별도 표기하는 선택지를 남긴다(이번 스코프 밖).
- **검증**: 다음 거래일 09:00 `DecisionEmitted`의 `gate`가 **`regime`이 아닐 것**.
  + `RegimeSeeded` 1건이 08:25대에 존재. + `RegimeClassified` ts < 같은 사이클 `DecisionEmitted` ts 유지.
- **적용 시점**: 장후. **별도 커밋** (F-1/F-2는 관측, 이것은 행동 변경이다 — 되돌릴 때 분리돼야 한다).
- **결정 필요 사항**: 없음. 단, F-1 적용 후 첫 거래일 관측에서 `n_experts=0`으로 판정되면
  **F-3보다 그쪽이 우선**이다(국면이 닿아도 입력이 0이면 첫 사이클은 여전히 접힌다).

### F-4. 점검 도구의 공백 임계를 프로세스 구동 주기에 맞춘다 — **P2** · 대응 이상점 1-3

- **변경 파일**:
  - `.claude/skills/messiah-daily-check/scripts/collect_evidence.py` — 로그 공백 판정부에
    프로세스별 기대 주기 테이블(`l1_daily: 60초`, `g2_daily: 1800초`)을 두고
    임계를 **`기대주기 × 2 + 5분`** 으로 계산. 테이블에 없는 프로세스는 현행 10분 유지.
  - 다이제스트 §5 표에 `기대주기` 열을 함께 출력해 **임계의 출처가 보고서에 남게** 한다.
- **회귀 위험**: 기대주기를 잘못 크게 잡으면 진짜 공백을 놓친다. 그래서 상수를 코드에 박지 않고
  **G-3에서 프로세스가 스스로 선언한 값**을 읽는 형태로 넘긴다(아래).
- **검증**: 오늘 다이제스트를 같은 인자로 재생성 → g2 공백 적신호 **8건 → 0건**,
  l1 공백 0건 유지.
- **적용 시점**: 장후. 점검 도구는 거래 경로가 아니지만 **커밋 규율은 동일하게 지킨다.**
- **결정 필요 사항**: 없음.

### 적용 순서와 커밋 계획 (전부 **장후 15:35 이후**)

1. **커밋 ①** — F-1 + F-2 (판단 관측 + 갈래 분리, 함께 넣어야 의미가 있다)
   `[MW0601] 접힌 이유가 두 개인데 문장은 하나였다 — 판단 갈래 값 계측 + n_experts 분리`
2. **커밋 ②** — F-3 (첫 사이클 국면 시드)
   `[MW0601] 판정은 나왔는데 0.5초 늦었다 — 웜스타트 직후 RegimeState 선발행`
3. **커밋 ③** — F-4 (+ G-3 1단계)
   `[MW0601] 30분마다 도는 프로세스에 10분 침묵은 이상이 아니다 — 공백 임계를 구동 주기에 맞춤`

각 커밋 전 `pytest`(해당 범위) + replay 검증 — 금지계명 2.

---

## 3. 고도화 방안

당일 관측에서 출발한 것만 쓴다.

### G-1. `decision_funnel`을 장중에 볼 수 있게 한다

- **관측 근거**: 오늘 12:36 `logs/status_snapshot.json`의 최상위 키는
  `code_version` · `components` · `circuit_breaker` · `irrecoverable_loss` · `command_center_ui` —
  **판단 계열 키가 0개다.** gate 분포는 `daily_integrity`(장후)에만 실린다.
  그 결과 *"8건 중 7건이 한 갈래로 접혔다"* 는 사실을 **장후에야 안다.**
  어제 결함 ②의 교훈이 *"측정 없는 수정"* 이었는데, 측정을 붙이고도 **보는 시점이 여전히 장후**다.
- **제안 내용**: `status_snapshot.json`에 `decision` 블록 추가 —
  `{"funnel": {"kill":0,"no_expert":0,"regime":1,"dispersion":0,"score":7,"pass":0},
    "last_decision_kst": "12:30:00", "cycles": 8}`.
  누적 카운터는 `MetaDecisionEngine`이 갖지 않고, 스냅샷 생성기가 당일 g2 로그의 `gate` 필드를
  세는 방식으로 한다 — **엔진에 상태를 심지 않는다**(단일 판정 경로 유지).
- **기대 효과**: 오늘 같은 "한 갈래 전량 정지"를 **12:30이 아니라 09:30에** 본다. 손쓸 폭이 6시간 늘어난다.
  (`ce51375`가 장중 점검을 13:30→12:30으로 당긴 것과 같은 취지의 다음 걸음이다.)
- **비용·위험**: 낮음. 읽기 전용 집계, 거래 경로 무영향. R18 무관(게이트 아님).
- **선행 조건**: **F-1·F-2** — 갈래 이름이 확정돼야 분모가 안정된다.
- **우선순위 제안**: 이번 주.

### G-2. 반복 ERROR 접기 — 장전 G-2 **이월** (신규 아님, 근거만 갱신)

- **관측 근거 갱신**: 장전 12건 → **12:33 기준 51건**(08:23:20~12:33:20, 5분 주기 × 250분 = 51 정확).
  15:35까지 **약 85건** 궤도. payload가 `{"underlying":"KOSPI200","series":"weekly_thu","legs":194,
  "nearest":"위클리C 2608W3   910.0"}` — 51건이 **한 글자도 다르지 않다.**
- **제안 내용**: 장전 결정 그대로 — `src/messiah/core/logging.py`에 `(tag, payload_hash)` 반복 억제.
  첫 1건은 원래 레벨, 이후 N분마다 `{tag}Repeated {n}회` 요약 1건. 해시가 바뀌면 즉시 복귀.
  **`WARNING→DEBUG` 강등은 하지 않는다**(08-07의 실수 — 소리를 줄이되 심각도를 왜곡한다, R6).
- **오늘 추가된 근거**: 오늘 l1 ERROR **51건이 전부 이 태그 하나**다. ERROR 카운트가 사실상
  이 한 사건의 중복도를 재는 눈금이 되어, **다른 ERROR가 섞여 들어와도 눈에 안 띈다.**
  장전엔 "시끄럽다"였는데 오늘은 **"가린다"** 가 됐다.
- **우선순위 제안**: **장후 최우선 유지.** 장전 F-1(캘린더 판정식)이 V-1/V-3 미충족으로 보류된 동안
  이것이 유일한 방어다.

### G-3. 프로세스가 자기 구동 주기를 선언하고, 점검 도구가 그것을 읽는다

- **관측 근거**: 오늘 적신호 12건 중 8건(67%)이 g2의 30분 침묵 오탐(§1-3).
  **같은 형태의 오탐 4회째**다 — `daily-axes-measured` · `LaunchWindowRefused` ·
  08-12 postmarket `SessionEnd` · 오늘. 공통 구조는 *"점검 도구가 대상의 전제를 모른 채 일반 임계를 적용한다"*.
- **제안 내용**: `SessionStart` 로그의 구조화 필드에 **`cadence_seconds`** 를 추가한다
  (`l1_daily: 60`, `g2_paper: 1800`, `postmarket: null`=1회성). 점검 도구는 상수 테이블 대신
  **로그가 스스로 말한 값**을 읽어 임계를 정한다.
  장전 G-3(`ops/fix_verification.py` 등록부 `since` 필드)과 한 쌍으로 본다 —
  그쪽은 **시간 하한**, 이쪽은 **주기 하한**. 둘 다 "도구가 대상의 전제를 모른다"의 다른 얼굴이다.
- **기대 효과**: F-4의 상수 테이블이 **불필요해진다.** 새 프로세스가 생겨도 점검 도구를 안 고친다.
- **비용·위험**: 낮음. `SessionStart` 스키마 확장이라 구독자 영향 없음(추가 필드).
  다만 `cadence_seconds`를 **빠뜨린 프로세스를 세는 메타 검사**를 함께 둔다 —
  장전 G-3가 `since`에 대해 정한 것과 같은 규율이다(빠뜨리면 진짜 결함을 "판정 불가"로 덮는다).
- **선행 조건**: 없음. F-4를 이것으로 대체해도 된다(F-4는 이번 주 급한 대응, G-3는 정본).
- **우선순위 제안**: 이번 주.

### 로드맵 반영 제안

| 항목 | 현 로드맵 위치 | 제안 위치 | 사유 |
|---|---|---|---|
| F-1·F-2 (판단 관측) | 없음 (오늘 신설) | **W24~26 Meta Decision 범위 내 즉시** | 어제 결함 ②의 미완 부분. Risk·Sizer·OrderGateway 검증(W24~26 잔여)이 이것 없이는 착수 자체가 불가 |
| F-3 (국면 시드) | 없음 (오늘 신설) | 〃 | `9170ce8`의 잔여 12.5%. 같은 마디를 두 번 열지 않는다 |
| G-1 (장중 funnel) | 없음 | 이번 주 · 운영 관측 | `ce51375`(장중 점검 12:30 당김)의 연속 |
| G-3 (cadence 선언) | 장전 G-3와 한 쌍 | 이번 주 · 점검 도구 | 오탐 4회째 — 개별 대응 한계 |

---

## 4. 다음 관측 예정

### 오늘 장후(15:35 이후)에 답이 나오는 것 — 장전 V-* 에 추가

| ID | 관측 대상 | 판정 기준 | 비고 |
|---|---|---|---|
| **W-1** | `DecisionEmitted` 종일 `gate` 분포 | `regime` **1건뿐**(09:00 단건)이면 1-1이 "첫 사이클 단건"으로 확정. 2건 이상이면 더 넓게 틀린 것 | ★ 1-1의 확정 판정 |
| **W-2** | 종일 `④ \|S\|=` 값의 **분산** | 13사이클 전부 정확히 `0.000`이면 `n_experts=0` 가설이 크게 강해진다. 하나라도 `0.000`이 아니면 집계는 살아 있는 것 | 1-2의 정황 보강(확정은 F-1 필요) |
| **W-3** | `Regime=UNKNOWN` 종일 비율 (V-9 확정) | 13건 중 1건 = **7.7% < 50%** | V-9 최종 채점 |
| **W-4** | `InvestorFlowPollRetried` 종일 건수 | 4건에서 크게 안 늘면 장전 관측 ③의 "예산 여유 0"은 장전 창의 성질로 확정 → F-3 긴급도 하향 확정 | |
| **W-5** | `OptionChainCalendarViolation` 종일 누적 (V-2) | 약 **85건** ±5 (5분 주기 × 425분) | 벗어나면 주기가 아닌 다른 요인 |

### 다음 거래일(2026-08-14 금) 장전에 볼 것 — 장전 V-4·V-7에 추가

| ID | 관측 대상 | 판정 기준 |
|---|---|---|
| **W-6** | `DecisionEmitted`에 `n_experts` 필드 (F-1 적용 후) | **존재. 값이 0이면 1-2 = "입력 없음" 확정 / 1 이상이면 "진짜 우위 없음" 확정** ★ |
| **W-7** | 09:00 `DecisionEmitted`의 `gate` (F-3 적용 후) | **`regime`이 아닐 것** + `RegimeSeeded` 1건 08:25대 존재 |
| **W-8** | 다이제스트 §9 적신호 중 g2 공백 (F-4 적용 후) | **0건** |

---

## 5. dev_memory 반영

- `DECISION_LOG.md` 추가 항목: `## 2026-08-13 장중 — 어제 P0는 풀렸고, 그것이 가리던 둘이 나왔다 ([MW0601], 2026-08-13)`
- `NEXT_TODO.md` 추가 체크박스: **12건** (F-1~F-4 · G-1·G-3 · W-1~W-8 중 신규분)
- 완료 처리한 기존 항목: **없음** — V-9는 장중 잠정 통과라 15:35 이후 W-3으로 확정한다.
  섣부른 완료 처리를 하지 않는다.
- **커밋하지 않았다** (장중 · SYSTEM.md R11 / 금지계명 4). 문서 갱신만 수행.

---

## 자체 검증

- [x] 관측 구간(09:00~12:36)을 첫 줄에 명시
- [x] 아직 차례가 아닌 것을 결함으로 적지 않음 — §1-4에 8항목 분리
- [x] 모든 이상점에 로그 시각·파일명·인용 부착
- [x] 각 이상점이 SYSTEM.md 조항(R6·R10·R11·금지계명 12) 또는 설계 문구(Ver 2.0 §3.1·§3.2, `phases.md` D절)에 대응
- [x] dev_memory 기존 항목 중복 보고 없음 — `OptionChainCalendarViolation`(장전 결함 ①)·G-2는
      **이월로 명시**, 1-1은 `DECISION_LOG.md:4503`의 **후속**임을 명기
- [x] `FixVerificationRecurred` 0건 · `code_version.stale` false · 산출물 누락 없음(장후분은 §1-4로 분리) 확인
- [x] Fix 계획이 파일·함수·행 수준까지 구체적이며 전 항목 **적용 시점: 장후(15:35 이후)** 명기
- [x] 고도화 3종 전부 당일 관측 근거(적신호 67% 오탐 · ERROR 51건 단일 태그 · 스냅샷 판단 키 0개) 보유
- [x] **코드를 변경하지 않았다** — 본 점검은 읽기(`grep`/`sed`/`python3` 집계)와 문서 작성만 수행
- [x] dev_memory 갱신 완료 (커밋 없음)
